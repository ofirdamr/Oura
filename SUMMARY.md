# SUMMARY — Oura

**Stage:** Thirteen real screens ported and verified against the Stitch design (plus one screen designed fresh, not in the original export) — **MVP screen porting from the 42-screen Stitch export is now complete**: Photographer Dashboard (`/admin`), Guest Landing/Join (`/join`), Gallery Entry (`/gallery-entry`), Personal Gallery (`/gallery`), Festive Gallery (`/festive-gallery`), Minimal Gallery (`/minimal-gallery`, third gallery theme variant — stark dark bento-grid treatment with filter chips, grid/list view toggle, and a two-button share/download FAB), Photo Editor (`/photo-editor`, brightness/contrast/saturation/exposure sliders + auto-optimize + crop&rotate + branded frame/watermark preview, all wired to local state driving a real CSS filter/transform on the placeholder tile), the biometric-consent gate (`/consent`, designed fresh per the CLAUDE.md guardrail — sits between Gallery Entry and Personal Gallery, accept/decline wired to the live `POST /consent/:token`), Create New Event (`/admin/create-event`, from `create_event_desktop` — name/location/date form, local-state-only submit simulation), Branding Settings (`/admin/branding`, from `branding_settings_desktop_2` — logo upload, premium frame style picker with a live device-switchable preview, brand accent color + auto-watermark toggle), Barcode/QR Management (`/admin/qr-management`, from `barcode_management_desktop` — post-creation QR/link distribution screen with real working clipboard-copy on the event ID and gallery link), and AI Optimization (`/admin/ai-optimization`, from `ai_optimization_desktop_1`, deliberately trimmed to auto-only scope per PRD.md — live processing queue, accuracy metrics, a real drag-interactive before/after comparison slider; manual model/quality-tier controls from the source design intentionally omitted, see QA notes), and the 3D Gift Box Reveal (`/gift-reveal`, from `gift_box_reveal_desktop` — the last MVP screen and the only non-static one: a real Three.js + GSAP scene, a metallic dark box with crossed coral ribbons that idles with a slow spin, can be dragged to rotate, and opens on click/tap with a GSAP-choreographed lid lift + inner glow + a placeholder "photo card" rising out, then the "זיכרונות מהאירוע" memories gallery and a CTA into `/gallery`). Canonical Tailwind theme reconciled (Material-3-style tokens: `surface-container` tiers, `primary`/`on-primary`, `tertiary`, `error`, `success`). Material Symbols icons self-hosted via npm (no CDN). Shared `AdminShell` (header+sidebar) extracted for reuse across photographer-admin screens; shared `components/guest/BottomNav` extracted for reuse across guest-facing mobile screens (used by `/gallery`, `/festive-gallery`, `/minimal-gallery`). `universal-framework` skill added (`.claude/skills/`) alongside `hebrew-rtl-best-practices`, codifies the Token Economist gate referenced in `MISTAKES.md`. Founder provided real Supabase + Cloudflare (R2/Workers) credentials; backend foundation (schema migration + Worker API skeleton) stood up in parallel. Pushed to `main`.

**Backend now stood up (founder credentials received):**
- `apps/api` — Cloudflare Worker + Hono, **deployed live** at `https://oura-api.oura-events.workers.dev`. R2 binding `MEDIA` → existing `ouramedia` bucket. Supabase service_role client wired via Wrangler **secrets** (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `GUEST_TOKEN_SECRET`) — never in git. Registered the account's first workers.dev subdomain (`oura-events`) via the CF API so the Worker had a reachable URL.
- **Guest token issuance + consent gate now LIVE** (replaced the `/gallery/:token` 501 stub). Opaque event-scoped token = `base64url(JSON{event_id,guest_id,iat}).base64url(HMAC-SHA256)`, Web Crypto only (no JWT lib) — `apps/api/src/token.ts` (`signGuestToken`/`verifyGuestToken`/`tokenHash`). Only `SHA-256(token)` is ever stored (`guests.token_hash`); the raw token is never persisted. Three routes, verified end-to-end via curl against a throwaway event (then cleaned up):
  - `POST /events/:event_id/guests` → generates the guest id Worker-side, inserts a `guests` row, returns `{ token, event_id, guest_id }` (201); 404 if event missing. Optional `{ display_name }`. This is what the future QR-scan / manual-code entry flow calls.
  - `GET /gallery/:token` → 401 on invalid/tampered signature; resolves guest by `(event_id, token_hash)`; returns the event's general `photos` (culled excluded; `/media/<key>` URL is a stub, TODO real R2 signing) **regardless of consent**, plus `personal_gallery` = `{consent_required:true}` pre-consent (zero face data read) or `{consent_required:false, consented_at, photos}` once consented. **Consent gate enforced in code** — `face_embeddings` is only queried inside the consented branch.
  - `POST /consent/:token` → inserts a `biometric_consents` row (retention_expires_at NULL per PRD §8), idempotent via `unique(guest_id)` (returns `already` flag; handles concurrent-insert race). This is what the (parallel-built) consent-gate screen's "I agree" calls.
  - Verified: issue→201; pre-consent gallery→`consent_required:true`/no personal photos; consent→`already:false`; re-consent→`already:true`; post-consent gallery→`consent_required:false` with an (empty — no face pipeline yet) personal photos array; tampered & garbage tokens→401; issuance vs. nonexistent event→404. Grepped the whole tracked tree for every credential literal incl. `GUEST_TOKEN_SECRET` — no leaks.
  - Fixed a dormant Worker bug found here: the `SUPABASE_URL` secret had been `.../rest/v1/`, which supabase-js double-appends → every query 404'd. Re-set to the clean project base URL. (First time the Worker's Supabase client actually hit the DB, since gallery was a stub before.) See MISTAKES.
- `supabase/migrations/0001_init.sql` written and **applied to the live project**: MVP schema only — `events`, `guests` (opaque-token backed, no Auth user), `photos` (R2 keys only, no binaries), `face_embeddings` (pgvector `vector(512)`, HNSW cosine index, cluster `person_id`), `biometric_consents` (retention_expires_at deliberately NULL/undecided per PRD §8). RLS enabled+forced on all tables; photographers keyed on `auth.uid()`, no `anon` policies. Verified live: all 5 tables reachable via the service-role key (200), and the anon key gets an empty result on the same tables (RLS correctly denies it, not an error) — Worker uses service_role to bypass RLS for guest-facing reads.
- Applied via the Supabase Management API (`api.supabase.com`, HTTPS) using a founder-issued personal access token — the direct Postgres connection (port 5432) is unreachable from this sandboxed environment (outbound is HTTPS-only through the agent proxy), so the DB-password/psql route documented earlier doesn't work here; the Management API's `/database/query` endpoint does, since it's plain HTTPS. Founder should revoke that access token now that it's served its purpose (it's account-wide, broader than this one task needed).

**Design QA findings so far (fixed during port):**
- Dashboard: stray Arabic character in Hebrew copy ("بשלבי" → "בשלבי").
- Guest landing: hardcoded physical `left-4` positioning and a wrong-direction `arrow_forward` icon — both real RTL bugs in the source design, fixed with logical properties + a mirrored icon.
- Numbered screen variants (e.g. `dashboard_desktop_1/2/3`) are near-duplicate re-exports, not distinct states — picking the cleanest/most consistent one per screen rather than building all variants.
- `gallery_entry_desktop/screen.png` is actually a Branding Settings screen (folder/content mismatch, same class of bug as the earlier checkout/notification-center one) — ignored; only `gallery_entry_mobile` was ported. Its content is a genuinely distinct screen from Guest Landing (explains the auto face-match mechanism via a "how it works" modal, no event-specific preview photos), not a duplicate.
- Personal Gallery: a Hebrew sentence with an inline-styled number (e.g. "מצאנו 12 תמונות") loses its surrounding spaces at the bidi run boundary when the number sits in its own `<span>` inside RTL text — fixed with `unicode-bidi: isolate` on the span, per the RTL skill's number-isolation guidance. Watch for this on every future screen with an inline count/stat inside Hebrew copy.
- Minimal Gallery: unlike the earlier `gallery_entry` case, the mobile/desktop pair here is *not* a mismatch — both `oura_final_production_minimal_gallery_mobile` and `_desktop` genuinely depict the same bento-grid gallery concept (same filters, load-more, Photo Santos photography credit), just with different stock placeholder photos per breakpoint. However the desktop `screen.png` renders a literal debug string, `"Oura_Final_Production_Minimal_Gallery_Desktop"`, as a real on-page `<h1>` — a leftover filename used as copy, not intentional content. Not ported. Also: the source's fixed-position share/download FAB (physical `right-6`) was re-implemented with logical `end-4` per the established left/right-bug-fix pattern from Guest Landing.
- Photo Editor: a different flavor of the folder/content mismatch — `oura_final_production_photo_editor_mobile/_desktop`'s `screen.png` is a full event-gallery page (matches Festive Gallery's concept, not an editor). The actual editor controls (brightness/contrast/saturation/exposure sliders, auto-optimize, crop&rotate, add-branded-frame) exist in `code.html` but only inside a hidden lightbox side panel toggled by JS, so they never render in the static screenshot. Ported the panel's controls (merged from both the mobile and desktop variants, which have slightly different button sets) as their own full screen rather than a gallery-embedded drawer, per this task's explicit brief. Also found and fixed a new signed-number bidi bug (see `MISTAKES.md`) and an inverted on/off toggle-switch direction (caught via DOM measurement, not eyeballing, per the established Minimal Gallery lesson).
- Biometric-consent gate (`/consent`): no source screen at all (flagged missing in CLAUDE.md/PRD.md from the start) — designed fresh to match the existing dark-luxury guest card style (`/gallery-entry`'s pattern: logo header, radial-gradient backdrop, `bg-surface-container/60` card). Content deliberately distinct from Gallery Entry's "how it works" explainer: focuses on the consent-specific facts (what's compared, who sees results, that declining still allows browsing the general event gallery) plus a privacy-policy link and revocability footnote. Accept/decline both route (accept → `/gallery`, decline → `/festive-gallery`) after a stubbed `TODO` in place of the real `POST /consent/:token` call, which a separate parallel agent is wiring.
- Branding Settings: `branding_settings_desktop_1`'s `screen.png` is actually a mismatched Gallery Entry screen (same class of bug as `gallery_entry_desktop`, just the reverse pairing) — used `branding_settings_desktop_2` instead, which is genuinely Branding Settings. Built a custom toggle switch again (auto-watermark) and got the on/end vs. off/start direction right on the first attempt this time, verified via Playwright DOM measurement rather than eyeballing per the now-established lesson.
- AI Optimization: source is an elaborate "Platinum Core" manual control panel (model selection dropdown, manual retouch-strength slider, quality-tier radio buttons) plus a literal debug filename rendered as the on-page `<h1>` (same artifact class as Minimal Gallery desktop). Per PRD.md's MVP scope ("AI Optimization auto-only"), the manual controls were deliberately not ported — they imply a non-automatic mode that's out of scope, not just an oversight. Kept: live processing queue, accuracy metrics, before/after comparison. The comparison slider uses a native `<input type=range>` under the page's `dir="rtl"`; verified via a real Playwright pointer drag (not `.fill()`, which bypasses native directional behavior) that Chromium correctly mirrors range-input min/max for RTL, so it lines up with the logical-property-driven CSS overlay with no fix needed - initially looked suspicious from a static screenshot alone, confirmed correct by testing the actual interaction.
- Barcode/QR Management: straightforward port, no mismatch. Clipboard-copy on the event ID and gallery link is real (Clipboard API), not a static placeholder, verified by reading the clipboard contents back in Playwright.
- 3D Gift Box Reveal: another folder/content mismatch — only the **desktop** `screen.png` is the real guest reveal (hero box + memories gallery). The **mobile** `screen.png` is largely a mismatched product-sales page ("Oura Pro" hardware, ₪1,299, buy button, shop/discover/account bottom nav) with just the box widget embedded; per the "match screen.png, not folder name" guardrail the product-sales chrome was **not** ported. Built the reveal responsively from the desktop composition. Reimplemented the mockup's CDN Three.js/GSAP as real npm deps (`three`, `gsap`, `@types/three`) — chose raw Three.js + GSAP over react-three-fiber deliberately (bleeding-edge React 19.2/Next 16.2 stack + the open sequence is inherently an imperative GSAP timeline; keeps the r3f/drei React-19 compat surface out of the tree for one screen, and the scene lives in a single self-managed `useEffect` with full teardown — rAF cancel, GSAP kill, listener/ResizeObserver cleanup, GPU disposal + `forceContextLoss`). Component (`components/guest/GiftBoxReveal.tsx`) is dynamic-imported with `ssr:false` since WebGL/canvas APIs don't exist server-side. Tuned for Three r185 (lights use `decay:0` to keep the mockup's simple intensity model; swapped deprecated `THREE.Clock` for a plain `performance.now()` timer to avoid its console deprecation warning). Found + fixed a real RTL centering bug during QA: the instruction pill mixed logical `start-1/2` with physical `-translate-x-1/2`, which mis-centers under `dir=rtl` and clipped the pill inside the `overflow-hidden` canvas on mobile — fixed with `inset-x-0 mx-auto` on a `w-max` element (see MISTAKES).

**Guest flow now wired end-to-end (was static/unlinked before):**
- New `apps/web/lib/api.ts` (typed client for the live Worker: `issueGuestToken`/`getGallery`/`postConsent`, discriminated `ApiResult<T>` so callers never need try/catch) and `apps/web/lib/guestSession.ts` (localStorage-backed guest session — chosen over a cookie since the token travels as a URL path segment to a different origin, not something a cookie would auto-attach; survives reload/reopen within the same event, which sessionStorage wouldn't).
- `/gallery-entry`'s manual-code button now calls `POST /events/:event_id/guests` and routes to `/consent`. **Known gap, not built yet:** there's no real "resolve a human-readable event code to an event_id" endpoint, so this currently issues against a hardcoded placeholder event id regardless of what the guest types — clearly marked with a TODO in the code.
- `/consent` now calls the real `POST /consent/:token` on accept, then routes to `/gallery`; decline routes to `/festive-gallery` **without** calling the consent endpoint (declining must never create a consent record).
- `/gallery` now fetches `GET /gallery/:token` for real. **Guardrail enforced here too, not just by `/consent`:** if the response says `personal_gallery.consent_required`, `/gallery` redirects to `/consent` itself rather than trusting that the guest arrived via the intended flow — a guest hitting this URL directly (bookmark, back button, shared link) still cannot see personal results pre-consent. An invalid/stale token (401/404) clears the local session and bounces to `/gallery-entry` rather than showing a dead-end error.
- `/festive-gallery` and `/minimal-gallery` are still static placeholder UI (deliberately out of scope for this pass — they're the general, non-personal event view).
- Verified with Playwright by mocking the Worker's responses (the sandbox's outbound proxy doesn't route through to the live Worker from a launched browser instance the way `curl`/Node do — logged in `MISTAKES.md`): full click-through (entry → consent → gallery) works, and — critically — navigating directly to `/gallery` with a mocked `consent_required:true` response correctly redirects to `/consent` instead of rendering anything.

**Real brand logos now in place (founder-provided, replacing every "O" placeholder):**
- Founder sent the real Oura logo (coral rose/camera-shutter icon + wordmark) and Photo Santos studio logo (gold/black camera+gift icon + Hebrew wordmark). Both source PNGs were named "_transparent" but had no real alpha channel — a checkerboard pattern was baked into the pixels instead (a common export artifact from some AI image tools). A first background-removal pass used a hard per-pixel on/off cutoff, which killed the anti-aliased edges and looked visibly rougher than the original — caught and redone with a smooth/continuous alpha reconstruction (desaturation + distance-to-checkerboard-gray scored on a soft ramp, not a hard threshold) plus a border-connected-component guard for the Photo Santos logo specifically (protects its legitimate silver-gray lettering from being mistaken for background). A denoise pass (connected-component size filter) also cleared faint whole-canvas grain the raw soft-alpha version left behind.
- Final assets: `apps/web/public/brand/{oura,photo-santos}-{icon,lockup}.png` (icon = mark only, for compact square badges; lockup = icon+wordmark stacked, for hero placements) — cropped tightly with even padding, RGBA, clean edges verified on both dark and light backgrounds (a faint bevel-shadow artifact from the original artwork is visible only on light backgrounds, irrelevant since the whole product is dark-themed).
- New shared `apps/web/components/brand/OuraLogo.tsx` and `StudioLogo.tsx` (both wrap `next/image`). Wired into `AdminShell` (propagates to all 5 photographer screens at once) and individually into every guest screen (`/`, `/join`, `/gallery-entry`, `/consent`, `/gallery`, `/festive-gallery`, `/minimal-gallery`, `/photo-editor`) plus the Branding Settings logo-upload/live-preview area. Per the founder's explicit brand-separation requirement: the two logos never share a label — each always appears with its own name/wordmark, never the other brand's, so which mark belongs to which brand is never ambiguous.

**Stage 1 (demoable MVP) — DONE, live end-to-end:**
A dedicated Plan agent worked out a staged rollout: since no photographer auth exists anywhere in `apps/web` (the schema requires `events.photographer_id NOT NULL → auth.users`), wiring Create Event for real was deferred rather than gating the first demo on it. Instead the entire *guest* path was made genuinely real, with one event seeded by hand:
- `GET /media/:key` — real R2 streaming route (`apps/api/src/index.ts`), replacing the old `/media/<key>` stub. Absolute URLs built from the request origin.
- `POST /events/:event_id/photos` — multipart ingest endpoint; a photographer (or, for now, a founder script) uploads files directly, Worker writes to R2 and inserts the `photos` row (`status:'ready'`, no processing pipeline yet).
- `GET /events/by-code/:code` — resolves a human event code to an `event_id`. New migration `0002_event_code.sql` adds `events.code` (partial unique index). `/gallery-entry` now resolves real codes instead of a hardcoded placeholder id, and also accepts `?code=` as a query param so a scanned QR skips manual typing entirely.
- Gallery grid (`/gallery`) now renders real photos via `next/image` against the Worker's media route instead of a static placeholder icon tile.
- **`apps/web` deployed publicly for the first time** — `https://oura-web.oura-events.workers.dev`, via the OpenNext Cloudflare adapter (`@opennextjs/cloudflare`), same platform as `oura-api`/R2/CDN to keep media-heavy bandwidth on one vendor. Previously the frontend had only ever run in local dev/build inside the sandbox.
- **One real demo event seeded** on the live DB: founder `auth.users` row created via Supabase Admin API (`ofirdamr@gmail.com`, no password set yet — photographer auth/login doesn't exist yet, this row only satisfies the FK), `events` row `code:"WED-2024"`, Photo Santos branding, 17 real wedding photos (founder's own Google Photos album) ingested via the new endpoint. QR encodes `https://oura-web.oura-events.workers.dev/gallery-entry?code=WED-2024`.
- Verified: `tsc` clean on both apps, production builds, Playwright walkthrough (mocked Worker) through bad-code/empty-code/real-code → consent → gallery, plus live `curl` verification of the real seeded event end-to-end (code resolve → guest token → gallery fetch → 17 real photo URLs → actual JPEG bytes streamed from R2).
- Personal/face-matched gallery is honestly empty/consent-gated — the face-embedding pipeline is Stage 2, not required for this demo.

**Stage 2 (next, not started):** self-hosted InsightFace/ArcFace face-embedding pipeline (Fly.io/Cloud Run), new selfie-capture screen post-consent (no design source yet — needs a Stitch prompt per CLAUDE.md), gift-reveal wired as the real post-consent transition.

**Stage 3 (after that):** real photographer Supabase Auth + login UI, Create Event/Branding Settings/QR Management wired for real persistence, Photo Editor real save/compositing — zero direct DB inserts needed to onboard a new photographer.

**Blocking questions (see `PRD.md` §8):** biometric consent/retention policy, final ILS pricing, print fulfillment partner choice.
