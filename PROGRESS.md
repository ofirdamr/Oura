# Progress Log

### 2026-07-03
- Gathered requirements (founder brain-dump), competitor brochure (LOCA) + live pricing page, and full Stitch design export (42/42 screens confirmed present across 5 zips).
- Read brand spec + 6 representative screens to ground architecture decisions in real design (gift-box = real Three.js/GSAP scene, Platinum tier already named in design, personal gallery tags photos by event moment, notification center includes a misidentification-report moderation queue).
- Found and flagged a design-export naming mismatch (a "checkout" folder contained Notification Center markup) — `screen.png` is the source of truth per screen going forward.
- Wrote and got approval on the architecture/roadmap/PRD plan.
- Created MD operating files: `CLAUDE.md`, `PRD.md`, `SUMMARY.md`, `PROGRESS.md`, `MISTAKES.md` (incl. owed entry for skipping the Token Economist gate twice during planning).
- Copied full Stitch export into `design/screens` (63 folders, all 42 concepts covered, code.html + screen.png each).
- Scaffolded `/apps/web` (Next.js App Router, Tailwind v4, TypeScript). RTL (`dir="rtl" lang="he"`) root layout, self-hosted Rubik/Hanken Grotesk via `next/font`, brand color tokens from the design spec. Build + dev server verified with a Playwright screenshot (dark theme, RTL, Hebrew copy all correct). Committed and pushed to `main`.
- Added the `universal-framework` skill (pulled from `ofirdamr/claude-skills-`) to `.claude/skills/`, alongside the existing `hebrew-rtl-best-practices` skill. This was referenced in `MISTAKES.md` but had never actually been committed to the repo.
- Ported Gallery Entry screen (`/gallery-entry`) from Stitch design. Found `gallery_entry_desktop/screen.png` is mislabeled (actually a Branding Settings screen) — ignored, ported `gallery_entry_mobile` only. Includes the "how it works" face-match explainer modal from the source design (informational only, no consent capture). Verified with Playwright screenshots (base + modal state) against `screen.png`, `tsc --noEmit` clean. Committed and pushed to `main`.
- Ported Personal Gallery screen (`/gallery`) from `personal_gallery_mobile`, using placeholder photo tiles with match-percentage badges (no real event media yet). Added shared `.glass-panel`/`.no-scrollbar` utilities to `globals.css`. Hit and fixed a bidi space-collapse bug around an inline-styled number in Hebrew copy (`unicode-bidi: isolate`). Verified with Playwright screenshots (base + filter-chip interaction) against `screen.png`, `tsc --noEmit` clean, no console errors. Committed and pushed to `main`.
- Extracted shared `components/guest/BottomNav` (used by `/gallery`) and ported Festive Gallery screen (`/festive-gallery`) from `festive_gallery_mobile_1` (full event gallery, "festive" branding theme with hero image, social share bar, tap-to-select multi-download). Added `.masonry-grid`/`.masonry-item` utilities to `globals.css`. Verified with Playwright screenshots (base + selection state) against `screen.png`, `tsc --noEmit` clean, no console errors.
- Founder provided real Supabase + Cloudflare (R2, Workers) credentials, unblocking the backend. Kicked off backend foundation and the Minimal Gallery screen port in parallel via background agents (Hybrid orchestration: Opus for backend, Sonnet for UI), each in an isolated git worktree to avoid colliding with this session's own commits.

### 2026-07-04 (backend stand-up — founder credentials received)
- Wrote `supabase/migrations/0001_init.sql`: MVP schema (events, guests, photos, face_embeddings, biometric_consents) + `vector`/`pgcrypto` extensions, HNSW cosine index on `vector(512)`, RLS enabled+forced with photographer-only `auth.uid()` policies and no `anon` policies (Worker uses service_role to bypass). `retention_expires_at` left NULL/no-default per PRD §8 (open legal question), documented in a SQL comment. NOT applied to the live DB — the anon/service_role keys are PostgREST JWTs and can't run DDL; founder must paste it into Supabase Studio SQL Editor.
- Scaffolded `apps/api` (Cloudflare Worker + Hono + @supabase/supabase-js). `wrangler.toml` binds R2 `MEDIA` → existing `ouramedia` bucket; `GET /health` + `GET /gallery/:token` (501 stub). `tsc --noEmit` clean.
- Deployed live: `npx wrangler deploy` → `https://oura-api.oura-events.workers.dev`. Account had no workers.dev subdomain and wrangler can't register one non-interactively, so registered `oura-events` via the CF API (`PUT /accounts/:id/workers/subdomain`) then redeployed. Set `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` as Wrangler secrets (piped trimmed values, never echoed). No secret values in the committed diff.
- Ported Minimal Gallery screen (`/minimal-gallery`) from `minimal_gallery_mobile`/`_desktop` (third gallery theme variant: stark bento-grid layout, filter chips, grid/list view toggle, share/download FAB). Confirmed the mobile/desktop pair is a genuine match (not a folder mismatch) but the desktop `screen.png` has a literal debug-filename string rendered as its `<h1>` — not ported. Reused shared `BottomNav`; re-implemented the source's physical-`right-6` FAB position with logical `end-4`. Verified with Playwright screenshots (base, scrolled bento/footer, and filter+view-toggle interactive state) against `screen.png`, `tsc --noEmit` clean, no console errors.
- Applied `0001_init.sql` to the live Supabase project. Direct Postgres (port 5432) is unreachable from this sandbox — outbound is HTTPS-only through the agent proxy — so the DB-password/psql route doesn't work here; used the Supabase Management API (`POST /v1/projects/:ref/database/query`, plain HTTPS) with a founder-issued personal access token instead. Verified: all 5 tables reachable via service_role (200), anon key gets an empty result on the same tables (RLS denies correctly). Founder advised to revoke the access token now that it's done its job.
- Implemented real guest token issuance + the consent gate in `apps/api` (was a 501 stub). New `src/token.ts`: opaque event-scoped token = `base64url(JSON{event_id,guest_id,iat}).base64url(HMAC-SHA256)` via Web Crypto (`crypto.subtle`, no JWT lib); `signGuestToken`/`verifyGuestToken`/`tokenHash` helpers. New Wrangler secret `GUEST_TOKEN_SECRET` (fresh `openssl rand -hex 32`, piped in, never echoed/committed). Three routes:
  - `POST /events/:event_id/guests` → 404 if event missing, else generates the guest id Worker-side (so it can be baked into the signed token), inserts a `guests` row storing only `SHA-256(token)` in `token_hash`, returns `{ token, event_id, guest_id }` (201). Optional `{ display_name }` body.
  - `GET /gallery/:token` → verifies signature (401 on invalid/tampered), resolves guest by `(event_id, token_hash)` + `guest_id` match (404 if no row), returns the event's general `photos` (culled excluded; `/media/<key>` URL stub, TODO real R2 serving) REGARDLESS of consent, plus `personal_gallery`: `{consent_required:true}` pre-consent (no face data read at all) or `{consent_required:false, consented_at, photos}` once consented. Consent-gate guardrail enforced — face_embeddings is only queried inside the consented branch.
  - `POST /consent/:token` → verifies token, inserts a `biometric_consents` row (retention_expires_at left NULL per PRD §8), idempotent via the `unique(guest_id)` constraint (`already` flag; also handles the concurrent-insert race). 
- Deployed live (`npx wrangler deploy`, trimmed CF creds). Curl round-trip verified end-to-end against a throwaway test event: issue→201; gallery pre-consent→`consent_required:true`, no personal photos; consent→`already:false`; consent again→`already:true` (idempotent); gallery post-consent→`consent_required:false` with an (empty, no pipeline yet) personal `photos` array; tampered token→401; garbage token→401; issuance against a nonexistent event→404. Test event/guest/consent/photographer rows deleted afterward (DB back to empty). Grepped the whole tracked tree for every credential literal used incl. the new `GUEST_TOKEN_SECRET` — zero hits, nothing leaked into the diff.
- Fixed a latent Worker bug found during verification: the `SUPABASE_URL` Wrangler secret had been set to a value ending in `/rest/v1/`, which supabase-js double-appends → every query 404'd (`lookup_failed`). Re-set the secret to the clean project base URL (`https://<ref>.supabase.co`). This was the first time the Worker's Supabase client actually hit the DB (gallery was previously a stub), so the bug had been dormant. Documented in README-DEPLOY + MISTAKES.

### 2026-07-04 (Photo Editor + biometric-consent gate)
- Ported Photo Editor screen (`/photo-editor`) from `oura_final_production_photo_editor_mobile`/`_desktop`. Found the folder's `screen.png` is actually a full event-gallery page (Festive-Gallery-like), not an editor - the real editor UI (brightness/contrast/saturation/exposure sliders, auto-optimize, crop&rotate, add-branded-frame) only exists inside a JS-hidden lightbox side panel in `code.html`, never rendered in either static screenshot. Per this task's brief, promoted that panel's controls (merged from both mobile/desktop variants) to their own full screen. All controls are genuinely wired to local React state driving a live CSS `filter`/`transform` on the placeholder tile (auto-optimize snaps to/from a preset, crop mode shows a real rule-of-thirds grid overlay, rotate buttons actually rotate the tile, frame toggle shows/hides a "PHOTO SANTOS" watermark overlay). Save is a local-only confirmation (no processing backend yet).
- Built the still-missing biometric-consent gate (`/consent`) - no source design exists for it; designed fresh to match `/gallery-entry`'s dark-luxury card style. Covers what face-matching compares, that only the guest sees their own results, and that declining still allows browsing the general event gallery. Accept routes to `/gallery`, decline routes to `/festive-gallery`, both after a stubbed `TODO` marking the real `POST /consent/:token` call a parallel agent is wiring - intentionally not guessing that contract.
- QA: found and fixed two real bugs during verification (not caught by initial eyeballing) - (1) a new bidi variant beyond the known space-collapse one: a *signed* number (`-40`) inside a `unicode-bidi: isolate` span still rendered reversed (`40-`) because the isolate's base direction inherited the page's `rtl`; fixed by adding `direction: ltr` alongside `unicode-bidi: isolate` on that span. (2) the auto-optimize toggle switch's on/off thumb position was backwards (behaved like an unmirrored LTR switch) - caught by measuring the thumb's actual computed `left`/`right` via Playwright DOM evaluation rather than trusting the screenshot at a glance, then fixed to the correct off=start/on=end logical convention. `tsc --noEmit` clean, no console errors, verified via non-fullPage Playwright screenshots (photo editor: base, auto-optimize+crop-grid interactive state, negative-slider-value state, save confirmation; consent: base, scrolled facts list, accept-pending spinner, and both accept/decline actually routing to `/gallery`/`/festive-gallery`).

### 2026-07-04 (guest flow wired end-to-end)
- Added `apps/web/lib/api.ts` (typed client for the live Worker API) and `apps/web/lib/guestSession.ts` (localStorage-backed guest token persistence — chosen over a cookie since the token is a URL path segment to a different origin, not something a cookie auto-attaches, and needs to survive reload/reopen within the same event).
- Wired `/gallery-entry` → `POST /events/:event_id/guests` → `/consent` → `POST /consent/:token` on accept → `/gallery` → `GET /gallery/:token`; decline routes to `/festive-gallery` without ever calling the consent endpoint. `/gallery` independently redirects to `/consent` if the API reports `consent_required`, enforcing the guardrail even for direct navigation, not just relying on flow order.
- Known gap, flagged with a TODO in code: no real event-code → event_id resolution endpoint exists yet, so manual code entry issues a token against a hardcoded placeholder event id regardless of the code typed.
- This session's dev-session hit an account-level rate limit mid-task (spawned as a background agent) and was cut off before its own commit/verification step, but the code itself was complete and correct: `tsc --noEmit` clean, and the orchestrating session verified live by mocking the Worker's responses in Playwright (the sandbox's proxy doesn't route a launched browser's real cross-origin requests through to the live Worker the way `curl`/Node do - see MISTAKES.md) - full click-through works, and direct navigation to `/gallery` pre-consent correctly bounces to `/consent`. Cleaned up leftover test events/auth-users (including some left over from the interrupted run) from the live Supabase project afterward.

### 2026-07-04 (Create New Event + Branding Settings)
- Ported Create New Event (`/admin/create-event`) from `create_event_desktop`: name/location/date form in a modal-style card over AdminShell, auto-barcode-generation preview, local-state submit simulation (idle → submitting → success) matching the source design's own front-end-only interaction script. Wired AdminShell's "אירוע חדש" button and "הגדרות" nav link, which previously pointed nowhere (`href="#"`).
- Ported Branding Settings (`/admin/branding`) from `branding_settings_desktop_2` (picked over `_desktop_1`, whose `screen.png` is actually a mismatched Gallery Entry screen — logged in SUMMARY). Logo upload dropzone, premium frame style picker (4 options) with a live desktop/mobile-switchable preview that reflects the selected frame, brand accent color picker, auto-watermark toggle. Got the toggle's on=inline-end/off=inline-start direction right this time, verified via Playwright DOM measurement (`boundingBox()` on the thumb in both states) rather than eyeballing.
- This round's two frontend/backend agents (Create New Event + Branding Settings, and guest-flow wiring) both hit an account-level session rate limit mid-task and were terminated. Recovered both: their in-progress code was complete and correct (`tsc` clean) even though cut off before their own commit step. Finished the remaining scope (Branding Settings, which the first agent hadn't started) directly rather than re-spawning a subagent, to avoid hitting the same limit again.
- `tsc --noEmit` clean, no console errors, verified live via Playwright (Create New Event's full submit flow incl. success state; Branding Settings' frame switching, device toggle, and toggle-switch direction).

### 2026-07-04 (Barcode/QR Management + AI Optimization)
- Ported Barcode/QR Management (`/admin/qr-management`) from `barcode_management_desktop`: post-event-creation QR/link distribution screen, print/display/download options (UI only), social share targets, and a real working clipboard-copy (Clipboard API) on both the event ID and the gallery link - verified by reading the clipboard contents back in Playwright, not just checking for a lack of errors.
- Ported AI Optimization (`/admin/ai-optimization`) from `ai_optimization_desktop_1`, deliberately trimmed to auto-only scope: the source is an elaborate manual "Platinum Core" control panel (model dropdown, manual retouch slider, quality-tier radios) which contradicts PRD.md's MVP scope of "AI Optimization (auto-only)" - omitted those controls on purpose, kept the live processing queue, accuracy metrics, and a real drag-interactive before/after slider. Source screen.png also has a literal debug filename rendered as its `<h1>` (same artifact class as Minimal Gallery) - not ported.
- Investigated a suspected RTL bug in the before/after slider (looked backwards from a static screenshot) by testing a real Playwright pointer drag rather than `.fill()` - confirmed Chromium correctly mirrors native `<input type=range>` min/max for `dir="rtl"` pages, which lines up with the logical-property CSS overlay with no fix needed. Worth remembering: verify actual interaction, not just a value set programmatically, before concluding something's broken.
- `tsc --noEmit` clean, no console errors on either screen, both verified live via Playwright.
- Investigated the founder's question about why every screen shows an "O" placeholder instead of the real logo: confirmed no logo asset exists anywhere in the repo, only external Google-hosted mockup image URLs referenced in the design's `code.html` files (not something to hotlink in production). Founder sent a raster logo concept (matches the swirl/camera-shutter icon already visible throughout the Stitch screens); used a border-connected flood-fill (not naive color-distance thresholding, which would've punched a hole through the icon's intentionally-dark aperture center) to remove the background cleanly, producing both an icon-only crop and a full icon+wordmark lockup. Found a real generation-artifact smudge in the source image itself near the top-right of the swirl - flagged to the founder with a zoomed crop, decision pending (regenerate / auto-patch / ship as-is).

### 2026-07-04 (real brand logos wired in everywhere)
- Redid the logo background removal properly: the first attempt (mentioned above) used a hard per-pixel alpha cutoff, which the founder correctly flagged as looking rougher/less sharp than the original artwork. Reprocessed from the original uploaded files with a smooth/continuous alpha score (desaturation + distance-to-checkerboard-gray on a soft ramp instead of a binary threshold), plus a denoise pass (connected-component size filter) to clear faint whole-canvas grain the soft-alpha pass left behind. Result is clean anti-aliased edges matching the source quality, verified by zooming into edge detail and compositing on both dark and light backgrounds.
- Final assets: `apps/web/public/brand/{oura,photo-santos}-{icon,lockup}.png`. New `apps/web/components/brand/OuraLogo.tsx` + `StudioLogo.tsx` (both thin `next/image` wrappers). Replaced every "O" placeholder across all 13 screens (AdminShell propagates to all 5 photographer screens at once; each guest screen updated individually) plus Branding Settings' logo-upload/live-preview area. Kept the two brands' labels strictly separate per the founder's explicit instruction (each logo only ever appears with its own name, never the other's) to avoid any ambiguity about which mark belongs to which brand.
- `tsc --noEmit` clean, no console errors on any of the 13 screens (verified via a full Playwright sweep), all routes return 200.
- Added a CLAUDE.md rule at the founder's request: never freehand-design new screens/UI directly - write a Stitch-ready prompt and hand it to him to run externally, then implement once he brings back the export. Doesn't apply to re-implementing existing `design/*` screens.

### 2026-07-04 (3D Gift Box Reveal — last MVP screen, MVP porting complete)
- Token Economist consult (per CLAUDE.md gate): leanest path = implement directly in this worktree, no subagents (single screen, already delegated to me). Model: Opus (real Three.js integration + verification judgement). Scope guard: build the reveal screen itself correctly (desktop screen.png is ground truth), responsive to mobile; do NOT port the mobile screen.png's product-sales chrome (mismatch); do NOT wire real inbound navigation from other screens this pass (note where it fits). Verify with Playwright at multiple animation points incl. console/WebGL error capture. Orchestration: solo.
- Ported the 3D Gift Box Reveal (`/gift-reveal`) from `oura_final_production_gift_box_reveal_desktop`. Confirmed the documented mismatch pattern again: the **desktop** `screen.png` is the real guest reveal (hero "משהו מיוחד מחכה לך" + 3D box + "זיכרונות מהאירוע" memories gallery + download CTA), while the **mobile** `screen.png` is mostly a mismatched product-sales page ("Oura Pro" hardware, ₪1,299, buy button, shop/discover/account bottom nav) with only the box widget embedded. Built from the desktop composition, made responsive; product-sales chrome intentionally not ported per the "match screen.png, not folder name" guardrail.
- Reimplemented the mockup's CDN `three.min.js` + `gsap.min.js` as real npm deps (`three@0.185`, `gsap@3.15`, `@types/three` dev) — no CDN tags per guardrail. `components/guest/GiftBoxReveal.tsx` holds the scene: dark metallic box body + lid + two crossed coral ribbons (colors mapped to the app's canonical `--color-primary` #ff8a75, not the mockup's older #9f402d), slow idle spin, pointer-drag to rotate, click/tap (below a 5px move threshold, so drag ≠ open) triggers a GSAP timeline — lid lifts + tilts, a scale pop, an inner PointLight glow ramps up, and a self-contained canvas-texture "photo card" placeholder rises out of the box and gently bobs. `navigator.vibrate?.(50)` haptic on open. Full teardown on unmount (rAF cancel, `gsap.killTweensOf`, listener + ResizeObserver removal, geometry/material/texture dispose, `renderer.dispose()` + `forceContextLoss()`).
- Library choice — raw Three.js + GSAP over react-three-fiber: the app is on a very new React 19.2 / Next 16.2 stack and the open sequence is inherently an imperative GSAP timeline (mirrors the mockup exactly); a single self-managed `useEffect` is the most robust integration and keeps the r3f/drei React-19 compat surface out of the tree for one screen. Stays within CLAUDE.md's stated "Three.js + GSAP".
- SSR handling: the scene is a client component dynamic-imported with `ssr:false` (WebGL/canvas APIs don't exist server-side) with a spinner fallback. The page is `"use client"` (needs `useRouter` + reveal state to swap the instruction pill and reveal the "כניסה לגלריה האישית" CTA). Where it fits the guest flow, noted in code + SUMMARY: consent-accept → this reveal → `/gallery`; real inbound wiring deferred (matches how sibling screens were left).
- Verified with Playwright (headless swiftshader WebGL) at both 1280×1400 desktop and 390×1600 mobile: real WebGL2 canvas mounts, screenshots at closed / mid-open / fully-revealed all render correctly, opened-state pill + CTA confirmed present, **0 console errors, 0 real WebGL warnings** (the only remaining console warnings were Playwright's own `ReadPixels` GPU-stall messages from screenshotting under software GL, not app code). `tsc --noEmit` clean.
- QA fixes during verification: (1) tuned lighting/materials for Three r185's physical light units (used `decay:0` point lights to keep the mockup's simple intensity model); (2) swapped deprecated `THREE.Clock` for a plain `performance.now()` timer to eliminate its console deprecation warning; (3) a real RTL bug — the instruction pill mixed logical `start-1/2` with physical `-translate-x-1/2`, which mis-centers under `dir=rtl` and clipped the pill inside the `overflow-hidden` canvas on mobile (showed "גר"); fixed with logical `inset-x-0 mx-auto` on a `w-max` element (logged in MISTAKES). All UI chrome uses logical properties + Rubik (no `--font-display` on Hebrew).
- **MVP screen porting from the 42-screen Stitch export is now complete** (13 ported + 1 designed fresh). Remaining work is backend/wiring only.

### 2026-07-04 — Stage 1 demoable MVP: real guest path end-to-end, frontend deployed, one real event live
- Product-sequencing decision delegated to a dedicated Plan agent (per the founder's standing process rule that judgment calls go to a "professional," not solo me): don't wire Create Event first (no photographer auth exists anywhere in `apps/web`, and the schema requires `events.photographer_id NOT NULL`) — instead make the guest path fully real and seed one event by hand. Full staged plan (Stage 1/2/3 boundaries) relayed to the founder before execution.
- Genuine hybrid execution: two independent subagents in parallel worktrees (backend on Opus: `GET /media/:key` R2 streaming, `POST /events/:event_id/photos` ingest, `GET /events/by-code/:code` + migration; frontend on Sonnet: `resolveEventCode` client + `/gallery-entry` wiring), each against a fixed API contract, no coordination needed between them. Both left work uncommitted in their worktrees per instruction; integrated by hand (patches applied to `main`, not a git merge, since neither subagent committed).
- Found during integration (not caught by either subagent): `/gallery` still rendered a static placeholder icon tile, never actually consuming `photo.url` — fixed directly (small/mechanical), added `next/image` `remotePatterns` for the Worker's media host.
- Deployed `apps/web` publicly for the first time via `@opennextjs/cloudflare` (OpenNext's Cloudflare adapter) — `https://oura-web.oura-events.workers.dev`. Chose Cloudflare over Vercel to keep media-heavy bandwidth on the same zero-egress platform as R2/CDN/the API Worker, and because Vercel's free tier is non-commercial and its Pro tier's bandwidth pricing is a worse fit for an image-heavy gallery app; also had Cloudflare credentials in-session already vs. needing a new Vercel account/token.
- Migration `0002_event_code.sql` applied to the live DB via the Supabase Management API (`/database/query`) using a fresh founder-issued personal access token (the one from the earlier session had already been revoked as instructed) — founder asked to revoke this one too now that it's done.
- Seeded one real demo event: founder `auth.users` row via Supabase Admin API (`ofirdamr@gmail.com`, no password set — photographer login UI doesn't exist yet, Stage 3), `events` row (`code:"WED-2024"`, Photo Santos branding, `status:'live'`), 17 real photos ingested via the new endpoint. Photos sourced from the founder's own Google Photos wedding album (shared link): `goo.gl` short links are fully dead now, but the resolved `photos.google.com/share/...` URL still serves a static HTML page with all `lh3.googleusercontent.com/pw/...` photo URLs embedded in an inline JS data blob (`=`-escaped) — extracted via plain `curl` + regex + JSON-unescape, no browser needed; picked 20 evenly-spaced across the full ~332-photo album for variety, 3 came back as expired/400 (Google's per-request auth on those URLs), kept the 17 good ones.
- Added `?code=` query-param support to `/gallery-entry` so a scanned QR (`/gallery-entry?code=WED-2024`) auto-fills and auto-submits instead of requiring manual typing — wrapped in `<Suspense>` since `useSearchParams` requires it in the App Router.
- Verified: `tsc` clean both apps, production builds, Playwright walkthrough (mocked Worker, since the sandbox's launched-browser proxy still can't reach real external hosts including our own now-live Worker — same known limitation as `photos.google.com` above) through bad-code/empty-code/real-code → consent → gallery; separately verified the *real* seeded event end-to-end via `curl` only (code resolve → guest token → gallery fetch → 17 real photo URLs → actual JPEG bytes streamed from R2, confirmed via `file`).
- Generated a real QR PNG encoding the deeplink, handed to the founder alongside the live URL — this is the actual "show customers an MVP" deliverable the founder asked for.

### 2026-07-04 (cont.) — Stage 3: photographer auth + admin CRUD wired for real
- Second PM/Plan-agent consult decided Stage 3 (photographer auth) before Stage 2 (face-matching): Stage 2's payoff is frozen behind PRD §8's biometric legal review (external, unscheduled), Stage 3 isn't and needs zero new infra since RLS was already `auth.uid()`-keyed. Founder calls: cookie sessions over localStorage, login/signup designed fresh (no Stitch round-trip), legal review deferred for now.
- First hybrid dispatch (3 parallel subagents: auth foundation/Opus, Worker endpoints/Opus, CRUD wiring/Sonnet) got killed almost immediately by an account usage limit — two of three left zero file changes (worktrees auto-cleaned), the third only got as far as `npm install`. Per the skill's resume-same-mode rule, retried the identical 3-subagent dispatch once the limit cleared; all three completed cleanly this time.
- Integrated all three (patches applied by hand, not git merge, since none of the subagents committed - matches the Stage 1 integration pattern). Found and closed one real gap during integration: the CRUD subagent had deliberately left the branding logo dropzone visual-only since the backend endpoint wasn't available to it yet - wired it for real once integrated (file input, upload via the new endpoint, real R2-served preview), plus fixed a React 19 `react-hooks/set-state-in-effect` lint error in two pages (calling `setLoading(false)` synchronously inside an effect for the missing-`event_id` branch - fixed by deriving the initial `loading` state from whether `event_id` is present instead).
- New hard skill rule added mid-session (user-requested): when a subagent fails or a task turns out more tangled than expected, the PM escalates to the highest available model (Opus/Fable) personally rather than just retrying cheap or quietly patching - but that's the exception path, and comes with an explicit obligation to actually check subagent output, not trust a self-report.
- Verified for real (not just typechecked): set a real password on the founder's `auth.users` row via the Admin API, confirmed login against the live project; cross-tenant RLS isolation confirmed with a real second photographer (zero rows leaked reading/writing another's event, both via PostgREST directly and via the Worker's ownership-gated logo endpoint returning a uniform 404); real logo upload round-tripped through R2; a full Playwright pass using the **real** session (not a fake token) so middleware's server-side `getUser()` validation was genuinely live, covering login → create-event → branding save → qr-management → logout, zero console errors. Both Workers redeployed with real `NEXT_PUBLIC_SUPABASE_*` values baked in at build time.
- Test artifacts cleaned up: throwaway RLS-test event and second test photographer account both deleted from the live project after verification.

### 2026-07-04 (cont.) — Working MVP milestone: real photo upload, event list, dashboard, scannable QR
- Third PM/Plan-agent consult of the session: audited the actual code (not just the summary) and found the decisive gap - a photographer could sign up, create an event, and brand it, but had NO browser path to upload a single photo. The ingest endpoint worked; nothing called it. Also found: `/admin` dashboard was 100% fabricated data (other photographers' fake events shown to every logged-in user), no way to find an event again after creation (dead `#` nav), and the QR "code" was a static icon, not scannable.
- Recommended fixing this loop next, not Stage 2 - every leg of the MVP's own success criterion is reachable today except face-find, which sits behind the still-deferred PRD §8 legal review anyway.
- Hybrid dispatch, 3 parallel subagents this time (all completed cleanly, no usage-limit interruption): backend `DELETE /events/:event_id/photos/:photo_id` (Opus), event upload/detail screen at `/admin/events/[event_id]` (Sonnet), event list + de-mocked dashboard + real `qrcode`-based scannable QR (Sonnet).
- Two subagents both touched `qr-management/page.tsx` (one wired its dead CTA, the other added the real QR generation) - merged by hand (applied the larger diff via patch, then manually re-applied the small CTA-link hunk on top, since patching both in sequence would have hit stale line-offset conflicts).
- Verified for real: full upload -> guest-gallery -> delete round-trip against the live seeded WED-2024 event (uploaded a real photo via the authenticated endpoint, confirmed it appeared in the real guest gallery fetch, deleted it, confirmed both the R2 object (404) and the guest gallery entry were gone). Both Workers redeployed.
- Added `docs/ARCHITECTURE.md` this session (didn't exist before, despite PRD.md promising it "once implementation begins") - covers backend/data architecture, frontend structural conventions (RTL, fonts, Three.js gift-reveal lifecycle, design-source rules), repo-layout reality check (`/packages/*` from CLAUDE.md doesn't exist yet), per-screen wired-vs-static status, the manual (no CI/CD) deployment process, and this project's actual verification conventions (curl for backend/RLS, mocked-Playwright for frontend logic, real-session-cookie trick for testing middleware). Codified as a hard rule in both this project's CLAUDE.md and the universal-framework skill (generalized for any project with real infra) - update it in the same commit as any structural change, going forward.
- `SUMMARY.md` fully rewritten (not appended) per the skill's own compaction rule, now that structural detail lives in ARCHITECTURE.md and full history in this file - kept short and current-state-focused for session handover.

### 2026-07-05 — Stage 2 build: face-matching pipeline (code complete, not deployed)
- Founder decided to proceed on Stage 2 ahead of formal legal sign-off: obtained an informal draft legal opinion from a lawyer-friend (recommends 30-day retention, active consent gesture, guardian/age confirmation), formal signed version still to follow, founder explicitly accepted the risk of building on the draft. Installed `israeli-privacy-shield` reference skill (`.claude/skills/`) for Israeli Privacy Law/Amendment 13 guidance as part of that decision.
- Planned via EnterPlanMode: 3 parallel Explore agents (guest-facing flow, backend schema/API, design assets) + 1 Plan agent for the technical design (queue architecture, clustering/matching thresholds, migration contents), before writing any code.
- Decided with founder: 30-day retention TTL; embedding service built as a portable container without committing to Fly.io vs Cloud Run yet; age/guardian confirmation folded into the existing `/consent` screen as an added checkbox rather than a new "age gate" screen (avoids a second Stitch round-trip for what's substantively one more consent gesture).
- Built: `supabase/migrations/0003_stage2_pipeline.sql` (retention trigger+backfill, `guardian_confirmed`, `embed_status`, `match_faces` pgvector RPC) — written, NOT yet applied to the live DB (needs a fresh founder-issued Supabase access token). `packages/processing-pipeline` (FastAPI + InsightFace `buffalo_l`, portable Dockerfile) — built and its HTTP-layer logic tested locally with the model stubbed (this sandbox's proxy allowlist blocks GitHub release downloads, so the real model weights were never fetched/exercised here; deploying to a real host is also blocked — no Fly.io/GCP credentials in this sandbox). `apps/api`: queue consumer (photo embed + clustering), scheduled retention cleanup (Cloudflare Cron), new `POST /guests/:token/selfie` (zero-retention by design — selfie/embedding never persisted, only the resulting guest_id link), `POST /consent/:token` now requires `guardian_confirmed:true`, export shape changed to `{fetch,queue,scheduled}`. `apps/web`: guardian-confirmation checkbox added to the existing `/consent` screen, `postSelfie()` client added — deliberately did NOT change `/consent`'s redirect target or build the real `/selfie` screen yet (no design source; a Stitch prompt was drafted and handed to the founder instead, per CLAUDE.md's no-freehand-design guardrail). `tsc --noEmit` clean on both apps.
- Updated `docs/ARCHITECTURE.md` (repo layout, schema, API routes, new §4a pipeline architecture, env/secrets, known gaps, deployment steps), `PRD.md` §8 (status: in progress/risk accepted), and rewrote `SUMMARY.md` in the same pass, per the hard rule.
- Explicitly deferred to a follow-up session (needs external inputs this session doesn't have): applying migration 0003 (Supabase token), deploying the embedding service to a real host, the real `/selfie` screen once the Stitch export comes back.

### 2026-07-05 (cont.) — Stage 2 deployed to infrastructure (embedding host still pending)
- Founder provided a fresh Supabase personal access token; applied `0003_stage2_pipeline.sql` live via the Management API (hit the same `SUPABASE_URL` has-`/rest/v1/`-baked-in gotcha and Cloudflare-creds leading-space gotcha documented in earlier Mistakes entries — both worked around the same way). Verified via PostgREST: zero NULL `retention_expires_at` rows, `photos.embed_status` present (defaulted `'pending'` on the 17 existing seeded photos), `match_faces` RPC callable.
- Created `face-embed-queue` + `face-embed-queue-dlq` live, set `EMBED_SERVICE_TOKEN` as a Wrangler secret, deployed `apps/api` — queue producer/consumer, daily retention cron, and the new routes are all live.
- Verified end-to-end against a throwaway test guest on the real `WED-2024` event (issued a guest token, confirmed pre-consent gallery/selfie state, confirmed `POST /consent` 400s without `guardian_confirmed` and 200s with it, confirmed `retention_expires_at` landed at exactly `consented_at + 30 days`, confirmed `POST /guests/:token/selfie` 403s pre-consent and fails gracefully with `embed_service_unavailable` (502, not a crash) post-consent since no embedding host is deployed yet) — then deleted the throwaway consent/guest rows.
- Updated `docs/ARCHITECTURE.md`/`SUMMARY.md` again to reflect infra-deployed-but-embedding-host-pending state, distinct from the earlier "code written, nothing deployed" snapshot.
- Remaining for Stage 2 to actually produce real matches: deploy `packages/processing-pipeline` to a real host (no Fly.io/GCP credentials in this dev sandbox) and the founder's Stitch export for the real `/selfie` screen.

### 2026-07-05 (cont.) — Real /selfie screen built from founder's Stitch export, deployed
- Founder returned the Stitch export (`oura_ai_desktop.html`/`oura_ai_mobile.html`). Built `apps/web/app/selfie/page.tsx` for real: `getUserMedia` live camera preview, capture-to-canvas, review/retake state, `postSelfie()` submission, routes to `/gift-reveal` on completion (matched or not), skip link to `/gallery`. Reused the established `getGallery` consent-check-on-mount guard (same pattern as `/gallery`), so a guest reaching this URL directly without consent gets redirected to `/consent`.
- Two real defects fixed during the port, not a literal copy of the export: (1) desktop export's capture-button Hebrew text was set to Hanken Grotesk (`font-headline-md`) — has no Hebrew glyphs, moved to Rubik (`font-sans`) per the standing CLAUDE.md guardrail; (2) dropped the export's CDN Tailwind/Google-Fonts `<script>`/`<link>` tags for the app's already-bundled equivalents (CLAUDE.md: no CDN tags in production). Also translated the export's local ad-hoc color tokens onto the app's real `globals.css` theme rather than copying class names literally — the export's `text-primary` means white, the app's `--color-primary` means the copper accent, so a literal copy would have silently recolored text.
- Verified with Playwright against a local production build (`next start`), using `--use-fake-device-for-media-stream`/`--use-fake-ui-for-media-stream` launch args to simulate a webcam (per the project's real-browser-can't-reach-real-hosts sandbox limitation, all API calls mocked via `page.route()`): confirmed RTL/layout, the camera → capture → review → confirm flow, navigation to `/gift-reveal`, zero console errors.
- Deployed `apps/web` (`opennextjs-cloudflare build` + `wrangler deploy`); confirmed `/selfie` returns 200 live. Deliberately did NOT flip `/consent`'s redirect from `/gallery` to `/selfie` or wire `/gift-reveal` into the sequence — the embedding service still has no real host deployed, so doing so now would put every real guest through a camera prompt for a feature that can't yet match anything. `/selfie` is reachable only by direct URL for now, same status as `/gift-reveal`.
- Host recommendation given to the founder for the remaining embedding-service deploy: Cloud Run over Fly.io, based on his stated constraint (free/near-free during the pre-revenue period, pay only once there are paying customers) — Cloud Run has a perpetual (not time-limited) free tier and true scale-to-zero; Fly.io discontinued its free tier in 2024 and now requires a payment method from day one. Flagged that this is a judgment call based on current knowledge, not independently re-verified against live pricing pages this session.

### 2026-07-05 (cont.) — Embedding service deployed to Cloud Run, Stage 2 fully live
- Founder walked through GCP account setup live (mobile Chrome, multiple screenshots): created project "Ouraforphotographers", linked billing, created a deploy service account. Ended up using GCP's default Compute Engine service account (`932309994000-compute@developer.gserviceaccount.com`) rather than the custom one originally intended - worked fine once it had the right roles.
- Real Dockerfile bug found and fixed: `insightface` (and transitive dep `stringzilla`) compile C/C++ extensions during `pip install`, needing a real compiler the `python:3.11-slim` base image doesn't ship. Moved to a multi-stage build (`build-essential` in a builder stage only, final image stays slim).
- Installed the `gcloud` CLI into this session's sandbox (not previously present); confirmed outbound HTTPS to `dl.google.com`/Google Cloud APIs works through the sandbox's proxy. Had to `unset CLOUDSDK_AUTH_ACCESS_TOKEN` (an ambient env var in this sandbox meant for something else) since it was silently overriding the service-account auth `gcloud` was configured with.
- Hit three incremental IAM permission gaps deploying via the default Compute service account key, each requiring the founder to add one more role through the console: Cloud Run Admin (to deploy), Artifact Registry Writer (to pull the built image on update), Service Account User (Google's standard requirement to run a service as a given identity, even acting as itself). Also enabled Cloud Run API, Cloud Build API, Artifact Registry API on the project.
- Deployed `oura-embed` to Cloud Run (`ouraforphotographers`/`us-central1`). Deployed first with `--no-allow-unauthenticated`, then reverted: Cloud Run's own IAM auth layer would reject the Cloudflare Worker's calls before they reached the app, since there's no VPC peering between Cloudflare and Cloud Run - granted `roles/run.invoker` to `allUsers` instead, relying on the app's own `EMBED_SERVICE_TOKEN` bearer check for actual access control (matches the design already documented in the service's own README). Enabled CPU boost to reduce cold-start latency while keeping `min-instances=0` (an early request hit a 502 from a cold-start timeout; confirmed resolved after enabling boost).
- Regenerated `EMBED_SERVICE_TOKEN` (the original value set earlier this session was never saved anywhere, Cloudflare secrets are write-only) and set it identically on both the Cloudflare Worker secret and the Cloud Run env var.
- Updated `EMBED_SERVICE_URL` in `wrangler.toml` to the real Cloud Run URL, redeployed `apps/api`. Verified live end-to-end against throwaway test guests on the real `WED-2024` event: consent (guardian-confirmed), selfie submission reaching the real deployed model, correct `no_face_detected` response for a real (no-face) test image - not a stubbed/mocked result.
- Flipped `/consent`'s redirect from `/gallery` to `/selfie` and confirmed `/gift-reveal`'s wiring from `/selfie`'s confirm-submit handler (both already built earlier this session, held back pending the embedding host). Updated stale header comments across `/consent`, `/selfie`, `/gift-reveal` that referenced the old "not wired yet" state. `tsc --noEmit` clean, deployed `apps/web`, confirmed all four routes (`/consent`, `/selfie`, `/gift-reveal`, `/gallery`) return 200 live.
- Cleaned up: deleted the GCP service-account JSON key from disk (both the uploads copy and the working copy) now that the deploy is done; founder advised to delete/rotate that key in the GCP console the same way Supabase access tokens get revoked after use.
- Rewrote `SUMMARY.md` fully (Stage 2 now genuinely complete, not partial) and updated `docs/ARCHITECTURE.md` (§1a, §4a, §7, §8, §9) to match the live state.
- Separately, added a standing rule to `universal-framework` (own commit): the Token Economist's scope-guard duty now explicitly covers conversation length itself, not just individual tool calls - proactively recommend a fresh session once MD files are current, rather than waiting to be asked.

### 2026-07-05 (cont.) — Live bug found and fixed: WED-2024 photos were never actually embedded
- Founder ran a real selfie against the live `WED-2024` event post-legal-signoff and got no match despite appearing in nearly every photo. Queried the live DB directly (Supabase REST via service-role key, already available in-session): all 17 photos still `embed_status:'pending'`, zero `face_embeddings` rows for the event.
- Root cause: these photos were seeded during Stage 1, before Stage 2's enqueue-on-upload existed — they were never queued for embedding, and no backfill mechanism existed to catch pre-existing photos up once the pipeline shipped.
- Added `POST /admin/backfill-embeddings` (`apps/api/src/index.ts`) — bearer-gated by a new `ADMIN_BACKFILL_TOKEN` Wrangler secret (generated and set live, not photographer auth, since no session credential for the founder's real account existed here), re-enqueues any photo not yet `embed_status:'done'` into the existing `face-embed-queue`. Reuses the already-correct queue consumer rather than duplicating embed/cluster logic. `tsc --noEmit` clean, deployed live.
- Ran it against `WED-2024`: 17 enqueued. Hit a real second bug mid-verification — a batch of photos hung at `embed_status:'processing'` for 15+ minutes with no error, no retry (confirmed via `wrangler tail` showing zero activity, and the Cloud Run `/health` endpoint responding in 0.4s throughout, ruling out a wholesale outage). Root cause: `embedClient.ts`'s `fetch()` to the embedding service had no timeout, so one stalled response blocked the whole queue invocation indefinitely instead of failing into Cloudflare Queues' existing retry/DLQ logic. Added a 25s `AbortController` timeout, redeployed, re-triggered the backfill.
- Final live state: 15/17 photos `done` (262 `face_embeddings` rows, 96 person clusters), 1 `failed`, 1 still hangs past the new timeout on retry — logged as a narrower follow-up in `MISTAKES.md` (the stall may not be only in the embed round-trip; R2 `.get()` and the Supabase RPC/insert calls have no timeouts of their own either). 15/17 is enough to fix the reported symptom — the founder should retest the selfie flow on `WED-2024` now.
- Updated `SUMMARY.md`, `docs/ARCHITECTURE.md` (§4, §7, §8), and `MISTAKES.md` in the same pass.
- Separately: founder confirmed the formal signed legal opinion has now been received (previously informal draft only) — updated `SUMMARY.md`/`docs/ARCHITECTURE.md` to close that known gap.

### 2026-07-05 (cont.) — Photographer self-service password reset shipped
- Built `/forgot-password` (request reset email via Supabase Auth's `resetPasswordForEmail`) and `/reset-password` (lands from the emailed link, waits for the auto-detected recovery session, then `updateUser({password})`), linked from a new "שכחתם סיסמה?" link on `/login`. Matches the existing dark-luxury auth-card pattern. `tsc --noEmit` clean, deployed live, both routes confirmed 200.
- Got the founder logged in immediately via the Supabase Admin API directly (bypassing email, since this project's outbound email deliverability isn't confirmed configured) - looked up the user id, set a new password, verified it live with a real password-grant token request before handing it over.
- Closes the "no photographer password-reset flow" known gap in `SUMMARY.md`/`docs/ARCHITECTURE.md`.

### 2026-07-05 (cont.) — Branding preview: background swap and enlarge-view now real
- Wired the 4 background-thumbnail swatches on `/admin/branding` to actually change the live preview (distinct gradient + icon per swatch), replacing a no-op that had zero visible effect when clicked. Reframed as "preview your frame/logo against a few different photo moods" per the design's intent.
- Wired "הגדל תצוגה" (enlarge view) to a real modal showing the same preview larger, with a working close button and backdrop-click-to-close.
- Verified interactively via Playwright (click swatch → screenshot shows the swap; click enlarge → modal opens; click close → modal closes, confirmed absent afterward), not just build/typecheck.
- Read `PRD.md` for the first time this session (should have been checked earlier) - corrects an assumption from earlier in the session: AI Optimization (auto-only) and Photo Editor (auto-optimize + frame) are explicit MVP scope per §4, not deferred/Phase-2 items. The three disabled AdminShell sidebar items (Archive, Customer management, Statistics) are correctly deferred though - Statistics & Analytics is explicitly Phase 2 per PRD, and Archive/Customer management aren't scoped anywhere in the MVP feature list.
- Added `PRD.md` to the universal-framework skill's MD table with an explicit rule: check it before any sequencing/scope call, don't infer "deferred" from a screen merely looking unfinished.

### 2026-07-05 (cont.) — Full RTL/design-fidelity audit across every screen, plus a real logo-upload bug
- Founder escalated hard mid-session: repeated visual bugs (icon order, alignment, missing edge-hugging) had each been "fixed" wrong at least once because the fix was reasoned about in the abstract or eyeballed from a screenshot instead of measured. Rebuilt `admin/events` for full design fidelity first (real search/status-filter/CSV-export/list-grid-toggle, replacing the bare table), verified via mocked-Playwright.
- Fanned out two background audit agents (admin screens; guest screens) to check every remaining screen against its actual `design/screens/*/screen.png` for the three known RTL bug patterns. Applied all confirmed findings: AdminShell header/sidebar/mobile-drawer icon-order and edge-hugging (went through two more wrong iterations before finally opening the design image directly and measuring `getBoundingClientRect()` — logged as its own Mistakes entry), `admin/create-event` modal header, `admin/events/[event_id]` page header, `admin/qr-management` (bottom action row, share-target grid order, icon+label buttons), `admin/page.tsx` recent-events row, stale `active="אירועים"` props that had silently broken the sidebar highlight on 4 pages, `gift-reveal` header, `gallery`'s matched-badge position.
- A second background audit agent (worktree-isolated) covered the rest: `admin/branding` (preview/settings columns were swapped left-right vs. design; logo row, accent-color, watermark row order), `admin/ai-optimization` (5 backwards rows plus a swapped before/after slider badge), `festive-gallery` (upload-count stat on the wrong edge), `gallery-entry`/`join` (icon/label order, plus a `material-symbols-outlined` gotcha: the icon font forces `direction:ltr` on itself, so `start-*`/`end-*` applied directly to an icon span resolves against LTR, not the page's RTL).
- Codified the fix as a permanent process change: `hebrew-rtl-best-practices/SKILL.md` §Step 8 now mandates checking the design image first, then measuring the live DOM (`getBoundingClientRect()`/`getClientRects()` on a `Range`), never trusting CSS reasoning or an eyeballed screenshot as proof.
- Separately, founder reported the studio-logo re-upload on `/admin/branding` silently doing nothing (old logo stayed). Root cause: the upload route stored every logo under a fixed per-event R2 key, but the shared `/media/*` route caches every key for a year as `immutable` — correct for content-addressed photo keys, wrong for a reusable logo URL, so a re-upload changed the R2 bytes but the browser/CDN kept serving the year-old cached response at the unchanged URL. Fixed by making the logo key content-addressed per upload (mirroring photos) and best-effort deleting the previous logo object afterward. `tsc --noEmit` clean, both Workers redeployed live.
- Updated the open PR (#2) description to reflect the full scope of everything on this branch (it had grown far beyond its original single-commit description), and rewrote the stale parts of `SUMMARY.md` (password-reset flow, this audit) in the same pass per the file's own snapshot rule.

### 2026-07-06 — Guest token expiry (code complete, deploy blocked on a fresh Supabase PAT)
- Token Economist consult: leanest path = implement directly, no subagents (single contained backend change: one migration + one shared resolver function + 3 near-identical call-site updates). Model: Sonnet. Scope guard: add expiry enforcement only; do not touch the URL-path token-transport design (a separate, larger fix) or attempt threshold-tuning/orphaned-screen work this pass. Orchestration: solo.
- Picked this milestone over the other two offered (wire/remove orphaned screens; tune match thresholds) because it's a flagged, unactioned security gap with a small contained fix, whereas threshold tuning has no real pilot match-rate data yet to tune against (only the hand-seeded `WED-2024` demo exists), and orphaned-screens is more of a product call.
- Design: rather than embedding an expiry claim in the signed token payload (would require reissuing every outstanding token to change the policy, and duplicates data already re-derivable from the DB), added `guests.token_expires_at` as a plain column (migration `0004_guest_token_expiry.sql`) defaulting to `created_at + 90 days`, checked in `resolveGuest()` (`apps/api/src/index.ts`) on the same row already fetched for the `token_hash` lookup — zero extra queries. 90 days chosen to comfortably outlast typical post-event gallery browsing while still bounding a leaked/logged token's lifetime; kept as a real column (not re-derived) so a specific guest's access could be shortened/extended later without reissuing their token.
- `resolveGuest()`'s return type changed from a bare `status` to a discriminated `{status, error}` (`invalid_token` | `token_expired` | `guest_not_found`) so the three call sites (`/gallery/:token`, `/consent/:token`, `/guests/:token/selfie`) can distinguish an expired token from a bad signature in the response body, not just collapse both into one generic `invalid_token` 401. All three call sites updated identically.
- `tsc --noEmit` clean on `apps/api`.
- **Deliberately did NOT apply the migration or redeploy `apps/api` this session** — no Supabase Management API personal access token was available (checked env vars and the filesystem; none present, unlike some earlier sessions where the founder provided one directly). Deploying the Worker code without the column existing first would 500 every guest route (`/gallery`, `/consent`, `/guests/:token/selfie`) on the new `select token_expires_at` — checked this failure mode before considering deploying, per the "verify a real query, don't trust presence checks" lesson from an earlier Mistakes entry, and concluded the safe order is DB-first, so left both undone together rather than deploying code the DB can't yet support. Documented as a clear next step (needs a fresh founder-issued PAT) in `SUMMARY.md`/`docs/ARCHITECTURE.md` §3/§4/§8, matching this project's established pattern for migration-blocked-on-credential handoffs (e.g. 0003's original write-up).
- Continued on the existing PR #2 branch (`claude/next-milestone-planning-k0wfne`) rather than a fresh branch — this session's assigned branch name (`claude/oura-next-milestone-lg3raq`) pointed at the same commit as PR #2's base with no PR of its own, while `k0wfne` already had the real up-to-date `SUMMARY.md`/`PROGRESS.md` and an open draft PR; forking a new branch off the stale base would have silently abandoned all of PR #2's unmerged work.

### 2026-07-06 (cont.) — Guest token expiry deployed live; a real reset-redirect bug found and fixed
- Ran a Plan/PM consult (per the founder's explicit push to route sequencing through the team rather than solo) on the open backlog (orphaned screens, threshold tuning, two Supabase Auth config gaps, the undeployed token-expiry migration). Its finding: the token-expiry deploy and the reset-redirect fix are the same blocker (one missing Supabase PAT), threshold tuning has no real data to tune against yet and shouldn't be manufactured (would itself be new biometric-adjacent retained data needing the same legal sign-off as the rest of Stage 2), and orphaned-screens needs a founder scope call (remove vs. build as real selectable themes, since `PRD.md` §4 lists them as separate MVP features) before any code, not a solo guess.
- Founder provided a fresh Supabase Management API PAT. Applied `0004_guest_token_expiry.sql` live (verified via `information_schema` that the column landed `NOT NULL` with the right default, and via a count query that all 8 existing `guests` rows backfilled correctly), redeployed `apps/api`, then verified live against a throwaway `WED-2024` test guest: fresh token → `200`, tampered token → `401 invalid_token`, and — to actually prove expiry enforcement rather than just trusting the code — directly backdated that test guest's `token_expires_at` via the Management API and confirmed the same token now correctly 401s with `token_expired`. Deleted the test guest row afterward.
- While fixing the documented "reset link redirects to localhost:3000" gap, found the doc was stale: `GET /v1/projects/:ref/config/auth` showed `site_url` was already correctly `https://oura-web.oura-events.workers.dev/` — someone/some earlier session had partially fixed this without updating `SUMMARY.md`. The actual live bug was different and previously undiscovered: `uri_allow_list` was `.../reset-passwordto` (a stray "to" appended, likely a fat-fingered dashboard edit), which never matched the app's real `redirectTo` value, so Supabase silently fell back to `site_url` (the homepage) instead of `/reset-password`. Fixed with `PATCH /v1/projects/:ref/config/auth {"uri_allow_list": "https://oura-web.oura-events.workers.dev/reset-password"}`, confirmed via a fresh `GET` (not just trusting the PATCH response echo).
- Updated `SUMMARY.md`/`docs/ARCHITECTURE.md` (§3, §4, §8) to reflect both fixes as live/verified rather than pending, and reminded the founder the PAT can now be revoked.

### 2026-07-06 (cont.) — Design-fidelity audit: real fixes, a real process failure (never deployed), then a real recovery
- Founder demanded pixel-perfect fidelity to the Stitch design across every screen. Ran the actual audit via 3 parallel subagents (guest-core, guest-secondary, admin) comparing live screenshots against `design/screens/*/screen.png`/`code.html` — found real, concrete bugs, not invented ones. Fixed ~20 across two commits (`ce5c0f8`, `242a929`): RTL grid-ordering (branding device toggle + frame-swatch grid + watermark logical-property bug, QR-management's whole two-column layout mirrored, AI-optimization's queue tile order), missing "Oura" wordmark lockups (join/gallery-entry/gallery/festive-gallery all had the bare icon instead of `OuraLogo`'s unused `variant="lockup"`), color drift (join's link color/arrow direction, festive-gallery's date badge — both sampled directly from design pixels), two drifted AI-optimization card titles, create-event's close-button side, gift-reveal's added label + two drifted copy lines.
- Rebuilt the screenshot-comparison artifact from a **local** build to show the founder — he rejected it correctly ("almost all what I told you wasn't changed"). Root cause, found by diffing `git show <commit>:<file>` against the actual live URL: **neither commit had ever been deployed.** The live site was still running the *first* audit round's build from 2026-07-05. Full lesson logged in `MISTAKES.md`.
- Recovery: deployed `apps/web` for real (`opennextjs-cloudflare build && wrangler deploy`), then proved it landed using two different methods depending on rendering mode — `curl` + grep for SSR'd/static routes (`/join`, `/festive-gallery`, `/minimal-gallery`, `/gift-reveal`), and md5-hashing the actual served `/_next/static/chunks/*.js` file against the local build's same file for client-rendered routes (`/gallery-entry`, `/gallery`, and the auth-gated admin pages) since those bail to `BAILOUT_TO_CLIENT_SIDE_RENDERING` and a plain curl shows nothing regardless of deploy status. All checks matched.
- Founder separately flagged the 3D gift box as "ugly." Traced it to the actual Stitch export's `code.html` (`design/screens/oura_final_production_gift_box_reveal_desktop/code.html`) — it has a real embedded Three.js scene definition (Stitch does specify a real look here, just in code, since a static screenshot can't capture a 3D scene) with ribbon/light color `#9f402d`, a previous session had swapped that for the app's generic brand coral "for consistency" (found the removed comment saying exactly this). Reverted to the actual source color + matched box material roughness/metalness/geometry exactly. Deployed, hash-verified live (commit `8df1b49`).
- Founder reported face-matching "not working, and it worked yesterday." Investigated with real DB queries, not guesses: confirmed his 2026-07-05 guest session has 11 real photos linked to one face cluster (still intact, nothing regressed); proved the live pipeline still works right now by submitting a real WED-2024 event photo through the actual live selfie endpoint and getting a correct match; found his 2026-07-06 session has zero links. Conclusion: the pipeline isn't broken, but couldn't determine from server-side data alone whether his selfie was never submitted (declined/backed out) or failed face detection — asked him to retry and report the exact behavior.
- Founder escalated hard mid-session (justifiably, given the deploy-gap discovery on top of the screenshot-tool's own earlier bugs) — told the session to stop, hand the situation to a Plan/PM agent, and said he wants to be told what the PM decides. Per the project's own hard rules (stop-and-re-consult when the orchestrating session is the one struggling; session handoff protocol), closed out with: `MISTAKES.md` entry on the deploy-gap failure mode written before anything else, `SUMMARY.md` rewritten with full honest current state and every open item explicit (not left for the founder to rediscover), all work committed and pushed, then handed the "what happens next" decision to a fresh Plan/PM agent rather than continuing solo.

### 2026-07-07 (guest-flow fidelity + fixes — gift-reveal, code entry, QR scanner)
- **Process failures up front (see `MISTAKES.md`):** did NOT run the mandatory Token Economist gate, PM working-mode consult, or formally load the `universal-framework`/`hebrew-rtl-best-practices` skills at session start; did not track conversation length. Founder (rightly) called this out. Also suggested *removing* the "coming soon" QR-scan button instead of wiring it — antithetical to the stated mission (wire Stitch's designed features), corrected after founder pushback.
- Merged PR #3 (guest-token-expiry docs/verification) — was still open from a prior session though already deployed live. Reset working branch onto the new `main`.
- **/gift-reveal accent color → rust.** Founder flagged the download button was coral, not the design's rust. Confirmed via design source: each guest/reveal Stitch screen defines `primary` as `#9f402d`, not the app-global coral `#ff8a75`. **Founder decision (AskUserQuestion): match each screen to its own Stitch source.** Scoped a rust `--color-primary` override on the gift-reveal page + fixed 3 hardcoded coral shadow/glow rgba values. Deployed, verified live (computed DOM color `rgb(159,64,45)`, live-HTML grep, screenshot).
- **/gift-reveal memories-header RTL.** Heading rendered left / button right — inverted vs `screen.png` (heading right, button left). The export's `code.html` says `flex-row-reverse` but its own rendered image shows the `flex-row` result; matched the image per guardrail. `sm:flex-row-reverse`→`sm:flex-row`. Verified by measuring live DOM (heading cx 1117 > button cx 131).
- **3D gift box rebuilt (founder: "very ugly").** Root cause: Stitch's metalness-0.8 material had no environment to reflect → renders flat black (even Stitch's own screen.png shows a near-invisible box). Rebuilt in `GiftBoxReveal.tsx`: RoomEnvironment/PMREM reflections, RoundedBoxGeometry body + overhanging lid, satin rust ribbons wrapping the sides, a real ribbon **bow** (knot + 2 loops + 2 tails) on top, NeutralToneMapping + neutral key light so the ribbon stays true rust (not orange). Founder approved the look.
- **Gift box "top should be open" on open.** The body was a solid capped box, so it looked closed after the lid lifted. Rebuilt the body as an **open-top container** (4 walls + floor + warm inner liner, no top face); the lid now lifts up-and-to-the-side (stays in frame, reads as removed) revealing the cavity as the photo rises out. Verified with open-state screenshot.
- **Guest code entry bug.** iOS autocorrect turned `wed-2024` into `We'd-2024` → never matched. Disabled autocorrect/autocapitalize/spellcheck on the input and added `normalizeCode()` (uppercase + strip non-`[A-Z0-9-]`) on input and submit. Verified live: messy `we'd-2024 ` → `WED-2024`.
- **Wired the in-browser QR scanner** on `/gallery-entry` (was a disabled `בקרוב` stub). Real camera scanner: `getUserMedia` rear camera → jsQR decode loop → normalizes the scanned deeplink/`?code=` → enters like a manual code; stops camera on decode/toggle/unmount; graceful permission-denied error. Added `jsqr` dep. Verified: fake-camera Chromium fed a QR y4m drove the real scan → filled `WED-2024`, no errors. **Mistake during this: `npm uninstall qrcode` removed a real dependency `admin/qr-management` uses and broke the build — reinstalled, logged in MISTAKES.**
- All the above committed (`761b38f`, `c444b6c`, `f21339f`) and **deployed + verified live** (live BUILD_ID matched local each deploy). Draft **PR #5** open with the full set. Founder rule reinforced this session: not "done" until commit → real `wrangler deploy` → verify live → report with a **link**.

### 2026-07-07 (cont.) — Per-screen rust color on the remaining guest screens; PR #5 merged
- Ran the mandatory session-start gate properly this time (the top MISTAKES lesson from earlier today): loaded `universal-framework` + stated the Token Economist line, loaded `hebrew-rtl-best-practices`, confirmed branch/clean state before any code. PM sequencing call: founder deferred ("PM decides, I want results") → took Item 1 (per-screen rust) as the leanest, highest-confidence visible win.
- **Merged PR #5** (gift-reveal rust/RTL/3D-box) — it was an open draft but already deployed live and verified, and its `/gallery-entry` rewrite (QR scanner + code-entry fix) is what the live site actually runs, so `main` was *behind* live. Per the project hard rule (a live+verified PR gets merged, not deferred) and explicit founder go-ahead, marked it ready and merged (`cc0e0e3`), then reset the working branch onto the new `main` so this work builds on the live code instead of regressing it.
- **Rust `#9f402d` applied to `/gallery-entry`, `/join`, `/festive-gallery`** — the founder's standing "match each screen to its own Stitch source" decision. Confirmed all three Stitch sources define `primary: #9f402d` + `on-primary: #ffffff` (not the app-global coral `#ff8a75`). Scoped a `--color-primary`/`--color-on-primary` override on each page root (same idiom PR #5 used on gift-reveal — Tailwind v4 `@theme inline` resolves every `*-primary` utility to the var), and fixed the two hardcoded coral `rgba(255,138,117,0.15)` radial-gradient glows on gallery-entry/join to `rgba(159,64,45,0.15)`. festive-gallery had no hardcoded coral, only the token override.
- **Deployed live** (`opennextjs-cloudflare build && deploy`, version `98f1a422`). Hit and fixed a real deploy-env bug: `CLOUDFLARE_ACCOUNT_ID` in this environment has a **leading space**, which malformed the wrangler API URL (`/accounts/ abf954…` → "No route for that URI"); trimmed both CF creds inline before deploy.
- **Verified live via the prescribed client-rendered-route method** (a headless browser can't reach the live Worker from this sandbox — proxy resets the connection, the known blind spot — so DOM measurement isn't available here): curled the two freshly-uploaded chunks (`37t2mrrp80bov.js`, `112pyoj_39ad2.js`) from the live URL → both HTTP 200, both byte-identical (md5) to the local build, and carrying the rust values incl. my exact unique `159,64,45,0.15` gradient and the `"--color-primary":"#9f402d"` override objects.

### 2026-07-07 (cont.) — Wired the /gift-reveal "memories" grid to real event photos
- Founder screenshots showed the reveal's "זיכרונות מהאירוע" grid as empty placeholder boxes and asked "where are my 17 photos." Root cause: that grid was a hardcoded `MEMORY_TILES` array of empty icon tiles, never wired to any data — so it looked empty for everyone, independent of face-matching. Confirmed the real photos are fine: live API `GET /gallery/:token` returns all 17 WED-2024 photos with loadable `/media` URLs (200, image/jpeg); the real personal gallery at `/gallery` already renders them. The reveal grid was just disconnected.
- Fix: fetch the general event photos on the reveal page the same way `/gallery` does (`loadGuestSession()` → `getGallery(token)` → `data.photos`), render them with `next/image` (media host already allowlisted in `next.config.ts`), keep the Stitch masonry layout template, and fall back to the placeholder tiles only when there's no session (direct-URL nav) so the celebratory reveal never collapses. Also made the per-tile hover download a real working `<a download>` link instead of the design's dead button (no new dead controls added).
- Deployed live (version `60ab94df`). Verified the new chunk (`0mmk_-btrf3os.js`) serves 200 and is byte-identical (md5) to the local build, carrying the new wiring. Visual confirmation of the tiles rendering is owed to the founder (headless browser can't reach the live Worker from this sandbox — documented blind spot); the API data + deployed code + reused `/gallery` render path make the outcome sound.
- Also created a throwaway probe guest ("probe-test") on WED-2024 while confirming the API; it holds no consent/selfie/biometric data (inert token_hash row). No Supabase PAT this session to delete it — flag for cleanup next time a PAT is issued.

### 2026-07-07 (cont.) — Simplified the 3D gift-box open animation (was rendering broken)
- Founder screenshot of the OPENED box looked broken: the lid + bow + ribbons hovered in-frame over the box and the photo looked trapped inside. Root cause: the open timeline lifted the lid only to `y:1.85` with an x/z offset + big rotation, so it stayed visible in a broken pose, and the card only rose to `y:1.2`.
- Fix (founder chose "simplify the existing 3D box"): gave the lid parts their own cloned (transparent-enabled) materials so the whole lid can fade independently of the body; on open the lid now lifts straight up and out of frame (`y:5.2`, minimal x/z, gentle tilt) AND fades its materials to 0 — reads as cleanly removed. The card rises well clear of the rim (`y:1.55`), comes forward (`z:0.6`) and scales up past full size (1.15) to be the unmistakable hero. Disposed the two new materials + added them to the gsap killTweensOf teardown.
- **Verified locally** before deploy (the honest way, given the sandbox can't reach the live Worker with a browser): ran `next dev` on localhost, drove the real reveal with Playwright (software-WebGL Chromium), captured closed + open states — closed shows a proper wrapped box + bow; open shows the lid fully gone and the photo risen out as the hero. Then deployed live (version `e1e51124`) and confirmed the GiftBoxReveal chunk (`35wau71hn5tna.js`) is served 200 and byte-identical (md5) to that locally-verified build. Founder to hard-refresh (in-app browser cached the old bundle).

### 2026-07-07 (cont.) — Gift box reworked to a real unwrap; strategic MVP-demo direction captured
- After the memories-grid fix, the founder tested the 3D gift box on his phone and it read as broken through several iterations. Reworked the open animation across ~8 rounds, verifying each with a software-WebGL Playwright capture of the LOCAL build (headless browser can't reach the live Worker from this sandbox). Final approved behavior ("this is the best"): on open the lid — with its ribbon cross and bow still on it — lifts off the top and stays visible to the side (nothing faded/deleted), the open box body stays with ribbon on its OUTER walls only (replaced the two solid bands that passed through the cavity with four wall-hugging strips, so no ribbon crosses the open interior), and the photo rises out. Deployed live, versions through `46293cde`; commits `eeab075`→`f187884`.
- Lesson worth noting for the log: "simplify/robustify" instincts (fade the lid, dissolve the whole box) repeatedly missed what the founder actually wanted, which was literal realism — see the box he approved. When a founder says "make it reality," model the real-world action (take the lid off, set it aside) rather than inventing a cleaner abstraction.
- Founder raised a strategic point: empty screens (no events/photos) look unattractive when showing the app to photographers; he wants the MVP finished to a demoable state. Captured in SUMMARY: the real `WED-2024` event is the best existing demo; first-run empty-state / sample-seed-on-signup is the actual gap. Orphan pages as a demo/showcase was floated then parked.
- All guest-flow polish (rust, grid, box) is live on PR #6. Conversation got very long (box iteration) — recommending a fresh session for the MVP-completion push.

### 2026-07-07 (cont.) — Predefined agent team + Orchestrator mode doctrine
- Founder wanted the team to be **real, reusable subagents** the orchestrator picks by name (not re-briefed each session), and asked what "Orchestrator mode" actually means mechanically. Built 11 predefined agents under `.claude/agents/` (roster in `.claude/agents/README.md`): leadership `pm-orchestrator` + `token-economist`, specialists `planner`, `backend-architect`, `frontend-rtl`, `qa-verifier`, `security-auditor`, `tooling-scout`, `copywriter-he`, `marketing` (dormant until launch). Each carries its role, model tier, tool scope, and the project guardrails from `CLAUDE.md` in-file.
- Rewrote `universal-framework` SKILL.md §0.4 modes to **Solo / Team / Hybrid / Orchestrator** (was Classic/Parallel/Hybrid), added §1a documenting the autonomous mission-loop-to-GOAL, when the PM stops to ask the founder, and how agents "talk" (relayed through the PM via SendMessage). Strengthened the model-choice rule with the real Sonnet-did-design-to-code-and-it-had-to-be-redone-on-Opus lesson (size by the hardest slice).
- Encoded the honest limits so the doctrine doesn't silently fail: subagents can't reliably spawn subagents (orchestration lives top-level, which embodies the PM); MCP/connector installs needing OAuth can't happen in a background session (tooling-scout surfaces them, but may add skills = files itself); an agent's tools are fixed in frontmatter (widening = a `tools:` edit applied by the PM). `planner` owns up-front edge-case interrogation (concurrency, double-buy, network loss, idempotency) so failure modes are designed out from the basics.

### 2026-07-07 (cont.) — Added the missing context-steward agent + per-mission model doctrine
- Coverage audit of the team: the one missing "wall" was the conversation-context / session-handover role (it only lived as scattered rules in token-economist §0.3 + PM handoff §0 + §6). Created `context-steward` (Sonnet) — watches conversation cost, and when it's long/wasteful parks the thread, updates the MD files, records open questions, flags a live PR to merge (surfaced, not merged silently — irreversible), and writes the next conversation's first message. Runs top-level (only the top level sees the live conversation); spawned to execute the handover once the trigger is recognized. Wired into README roster + SKILL.md §1.
- Encoded per-mission model assignment: token-economist now recommends a model **per sub-mission** (not one tier for the whole team), and the PM applies it via the `Agent` tool's `model` param, which overrides the agent file's frontmatter default for that spawn — so one run really can have Opus + Sonnet agents at once. Also documented that each real subagent runs in its own small context window (leadership roles run top-level by necessity). 12 agents total now.

### 2026-07-07 (cont.) — Made tool adoption independent (scout ledger)
- Founder wants the tooling-scout to work independently. Reality: skills/plugins are files → scout adopts them fully autonomously; only OAuth connectors need a one-time human "Allow" click (can't be done headless — unfixable). Made the workflow as independent as that allows: created `docs/TOOLING.md` ledger. Scout logs anything needing login there with the exact one-time step; on any later session it re-checks `ListConnectors` and the moment an item shows authorized it adopts it automatically (wires it into the relevant agents' tools:) without asking again. context-steward carries pending-login items into the next conversation's first message so they're never lost. Net: founder does one click per connector, ever; everything else is the team's.

### 2026-07-08 — Gallery full-screen viewer + branded per-photo download/share (live, branch `claude/oura-gallery-fullscreen-0246bo`)
- Built a full-screen, social-app-style `PhotoViewer` (`components/guest/PhotoViewer.tsx`) opened by tapping any `/gallery` thumbnail: pinch/wheel/double-tap zoom + pan, left/right swipe + arrow-key/chevron nav, RTL-correct chrome, per-photo download + share, videos via native `<video>`.
- Added a canvas branded-photo compositor (`lib/watermark.ts`): draws a sample photographer frame (color per frame style) + Photo Santos logo + event title onto a guest-friendly JPEG (never a raw ZIP). Taint-safe via fetch→blob→ImageBitmap. Per-photo download/share and the two previously-dead `/gallery` buttons ("download all"/"share") now use it; share is Web Share API with a file, falling back to save.
- API `GET /gallery/:token` now returns guest-safe `event: { name, branding: { event_title, logo_key, frame, primary_color, auto_watermark } }` so the client composites real photographer branding; falls back to `events.name` when `event_title` is unset.
- Admin `/admin/branding` gained a "share title" field persisted to `branding.event_title` (the composited title). Frame + logo already existed there.
- Added a dedicated `seo` agent (`.claude/agents/seo.md`, Sonnet) — technical/on-page SEO, privacy-first (token/admin/consent routes noindex). Wired into the roster README.
- Deployed both: `oura-api` (Version 74b55b19) and `oura-web` (Version 148b3662-7ed6-48c3-9cba-2074515a8c6e). Verified LIVE with Playwright against the real deployed URL (browser reaches the live Workers via a node-fetch proxy — the documented sandbox blind spot): 17 tiles, viewer opens, swipe→"5/17", wheel-zoom scale, and a live branded download (198KB) showing the crystal frame + logo + Hebrew event title "אירוע הדגמה - פוטו סנטוס". Admin field verified live via a mock-Supabase login build screenshot.
- Note: Cloudflare deploy from this sandbox needs `CLOUDFLARE_ACCOUNT_ID`/`CLOUDFLARE_API_TOKEN` whitespace-trimmed, and web must build with `NEXT_PUBLIC_SUPABASE_URL/ANON_KEY` mapped from the session's `SUPABASE_URL/ANON_KEY` (real project `voxxhvywzaizyputjqkm`).

### 2026-07-08 (cont.) — Gallery viewer UX rebuild after founder feedback (branch `claude/oura-gallery-fullscreen-0246bo`)
- Founder rejected the first viewer as "built to spec, not what a guest wants." Rebuilt for a premium, native feel:
  - `PhotoViewer` is now a horizontal **swipe carousel** (photos slide with motion, rubber-band at ends), with **swipe-up/down to dismiss** (Instagram-style; backdrop fades, X kept as hint), pinch/double-tap zoom + pan. Fixed a real bug: `dir="rtl"` reversed the flex track and pushed the current slide off-screen (killing gestures) — forced `dir="ltr"` on the track.
  - Each photo renders as a branded **magnet** (`BrandedFrame`): frame + logo + event title baked onto the IMAGE bottom (not the screen), full-bleed, WYSIWYG with the saved JPEG.
  - Save/share now target the phone: `lib/photoActions.ts` routes save → share sheet ("Save N Images" to Photos, not a Files dump) and share → sheet with a friendly caption and **no raw URL**.
  - Gallery grid is a **uniform** 3-col square grid (removed the random-height collage). Filters are now real/working (`all` vs `mine`), shown only when there are matches; removed the dead category chips.
  - Oura logo: removed the `bg-surface-container-high` wrapper box (the box, not the transparent PNG, was the visible square).
  - Removed the accent hairline from card + canvas for clean WYSIWYG parity.
- Encoded a mandatory **1-minute UX self-proof** ("think as the real user, not the spec") into `frontend-rtl.md`, `qa-verifier.md`, `universal-framework` §4, and `MISTAKES.md`.
- Verified live locally with Playwright (mobile viewport, real WED-2024 photos via node-fetch proxy): swipe advances 3→4, swipe-down dismisses, branded card + matching 184KB composited download. No API change this round.

### 2026-07-08 (cont.) — Gallery round 3: multi-select, marketing caption, solid nav, dead-button cleanup (live)
- Founder feedback pass. Shipped + verified live (oura-api af497a1c, oura-web cba581ca):
  - **Multi-select**: a "בחירה" toggle enters selection mode (coral ring + check per tile); a floating action bar shows "{n} נבחרו" with Save/Share of only the picked photos. Guests rarely want all — now they pick a few. Per-photo viewer actions still cover the single case.
  - **Marketing share caption**: share sheet is pre-filled with a promo caption (`חוגגים ב{event}! 📸 הצילומים באדיבות {studio}`) so a guest's share promotes the photographer; guest can edit. Photographer can set custom wording via new `/admin/branding` "טקסט שיתוף" field → `branding.share_caption` (API returns it in gallery event.branding; default built from title+studio).
  - **Bottom nav**: now solid opaque `bg-surface-container` + hard top border + safe-area padding (was a glass panel that bled page bg through on scroll).
  - **Removed** the dead gray notifications/profile header stubs.
- Note on Facebook: the caption is passed via the Web Share `text`; WhatsApp/Telegram/Messenger honor it, Facebook's feed composer may strip text on image shares (platform limitation, not ours).
- **Not built (need decisions / new Stitch screens):** (1) Prints & gifts commerce — designs exist (`premium_prints`, `checkout`, `order_confirmation`); real Stripe checkout is Phase 2 per CLAUDE.md. (2) Guest comments → event-manager wall + on-screen display — needs new Stitch screens (guest comment input, comments wall, event-screen display) since none exist; backend (comments table + endpoints) is buildable once the UI is designed. Both surfaced to founder for sequencing.

### 2026-07-08 (round 4) — Restored/wired header controls, wired all live-flow buttons (live, oura-web f1a0e00b)
- Corrected a mistake from round 3: I had REMOVED the notifications/profile header controls; founder (correctly) objected — designed buttons get wired, not deleted. Restored both and wired them: notifications → real panel (face-match status + event photo count), profile → real panel (event context + redo-selfie + leave-event). Verified live.
- BottomNav: every item now a real action (home/gallery → scroll top, share → select mode, profile → profile panel); no dead `href="#"`. gift-reveal footer wired (בית/גלריה → /gallery, תמיכה/פרטיות → mailto).
- Hooks check (founder asked): BOTH work. SessionStart (`session-start-enforce.sh`) fired (injected the MANDATORY OURA RULES at session start). Stop hook (`enforce-visual-qa.py`) runs each turn-end and only BLOCKS when an apps/web UI file was edited with no screenshot/Playwright evidence in the transcript; every UI change this session included a real Playwright screenshot, so it correctly passed instead of blocking. Self-tested the script (parses, exits 0). Not broken.
- Still-dead links, NOT in the live guest flow (orphaned/parked demo pages per founder): `minimal-gallery` (6 internal `href="#"`), `festive-gallery`/`join` content links. Left for a decision on those pages' fate (founder parked them earlier). Their shared BottomNav is now wired.
### 2026-07-10 — `media-ui-verify` skill + QR fullscreen fix + duplicate-PR cleanup (PR #18, branch `claude/awaiting-instructions-0f4pte`)
- Added `.claude/skills/media-ui-verify/SKILL.md` (7 deterministic checks: lint/typecheck, RTL, font, R2-vs-Supabase, consent-gate, design fidelity, live screenshot) and wired it into `CLAUDE.md` guardrails so it fires automatically before any `/apps/web` or `/packages/processing-pipeline` change is reported done.
- Wired `/admin/qr-management`'s "הצגה על מסך מלא באירוע" button to a real Fullscreen API view of the QR code. **Not locally verified** — this sandbox has no Supabase env vars, so `/admin/*` routes 500 at the middleware layer before reaching the page; code was reviewed manually only. Needs a real screenshot/click-test once deployed.
- Print (A4/stickers) and social share-target buttons on `/admin/qr-management` remain deliberately stubbed (real print layouts and share-target integrations are out of scope for this pass, same as before).
- Independently rebuilt `/gallery`'s download/share buttons from scratch (not knowing PR #10 already shipped this, live, on 2026-07-08 — see above) — **discarded during rebase in favor of the already-merged #10 version**, which is strictly more complete (branded frame/logo/event-title compositing, multi-select, real filters). See the 2026-07-10 cleanup entry below for the root cause and the process fix.
- **Fixed the deploy blocker**: `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` env vars carried stray whitespace, breaking `wrangler`'s auth header. Trimming them in-shell restored a working `wrangler whoami`/`deployments list`.

### 2026-07-10 (cont.) — Root-caused and fixed the PR #10 pileup (lost work + 3x duplicate rebuild)
- Founder reported the branded gallery download/share (built live on 2026-07-08) had "disappeared." Investigation: it never disappeared from git — PR #10 (the real, most complete version: full-screen viewer, branded frame/logo/event-title compositing, real filters, multi-select) was deployed live and verified on 2026-07-08 but **left open as an unmerged draft**. Two later sessions (PR #9, then #17) didn't check for existing open PRs before starting the same "wire the dead gallery buttons" task, each rebuilding a plainer, unbranded version from scratch; this session's own PR #18 did the same a third time. One of the later duplicate deploys overwrote the branded version on the live site, which is what made it look lost.
- **Fix applied:** merged #10 into `main`, closed #9/#17 as duplicates, rebased #18 to drop the now-superseded duplicate gallery work and keep only the genuinely new pieces (media-ui-verify skill, qr-management fullscreen).
- **Process fix (encoded in `CLAUDE.md`/`universal-framework` this session):** (1) merge is no longer an ask-first gate — once a change clears the verification it's actually capable of clearing, the PM merges it as part of closing out the mission, with revert as the safety net, instead of leaving it as an open draft waiting on a reply that may never come; (2) before starting any task that wires/fixes/builds a specific named feature, check `list_pull_requests`/`list_branches` for existing open work touching the same area first; (3) any question that genuinely does need the founder's answer gets written into `SUMMARY.md`'s open-questions section and every subsequent session must re-surface it before proceeding in that area, rather than letting it quietly drop when a session ends unanswered.
- Merged #10 and #18 to `main`, fixed the deploy-token whitespace bug, then deployed `oura-web` live. **Caused and immediately fixed a real incident in the process:** the first deploy from this sandbox built without mapping the session's `SUPABASE_URL`/`SUPABASE_ANON_KEY` to `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` before `npm run build`, so the shipped middleware bundle had no Supabase client config and every `/admin/*` route 500'd live for a few minutes. Caught it immediately via a route smoke-check, rebuilt with the env vars correctly mapped, redeployed — `/admin/*` routes back to their correct 307 (redirect-to-login when unauthenticated) instead of 500. Confirmed live via BUILD_ID match and an md5-identical JS chunk containing the qr-management fullscreen fix. **Lesson for next session:** `npm run deploy` from a fresh sandbox is not safe as a bare command — always export `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` from `SUPABASE_URL`/`SUPABASE_ANON_KEY` first, same as `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` whitespace-trimming — both are now load-bearing pre-deploy steps, not optional.

### 2026-07-10/11 (cont.) — Swept remaining open PRs; merged #15, #11; documented #16/#4/#7/#12 as still-open
- Founder's follow-up demand after the pileup fix: check for OTHER undocumented open PRs, and make "every unmerged PR gets named in SUMMARY.md, every session, no exception" a standing rule (encoded in `CLAUDE.md`). Found 6 more open draft PRs beyond #9/#17/#18 that were never surfaced anywhere: #4, #7, #11, #12, #13, #15, #16.
- **Merged #15** (context-guard `UserPromptSubmit` hook — hard-blocks at 35% context, would have fired on this very session) clean, no conflicts.
- **Merged #11** (the real face-matching retention-cron bug — cron was deleting shared `face_embeddings` rows instead of un-linking the guest, silently breaking matching for the whole event on every consent expiry) after resolving one conflict (`gallery/page.tsx`, superseded by #10, took main's version) — turned out to also carry two other already-built, real fixes that would've been stranded: logo-wrapper-box removal across ~10 screens, and a real guest-photo texture on the gift-box reveal card. `tsc`/`next build` clean, deployed live to `oura-api`. **Not done:** re-running `POST /admin/backfill-embeddings` against `WED-2024` — this session has no `ADMIN_BACKFILL_TOKEN`.
- **#13 merge attempt failed** (real conflict, likely `docs/ARCHITECTURE.md`/`PROGRESS.md` — not investigated under context pressure).
- **#16, #4, #7, #12 not attempted** — all have high conflict odds against files heavily rewritten this session (`SUMMARY.md`, `PROGRESS.md`, `MISTAKES.md`, `universal-framework/SKILL.md`) or need a fresh mergeable-state check (#12, stacked on the now-merged #10). Documented in `SUMMARY.md` per the new rule instead of force-resolving blind under a ~2.4MB conversation.
- **Also encoded:** state the concrete plan (not just Economist mode/model) before executing a mission, so the founder can redirect before tokens are spent.

## 2026-07-11 (cont.) — WED-2024 face-match investigation: RESOLVED, selfie matches; + DB delete-guard
- Founder sent his real selfie. Built `POST /admin/selfie-test?event_id=` (bearer `ADMIN_BACKFILL_TOKEN`, multipart `file`, zero-retention — writes/links nothing) and ran his selfie through the exact `embed→match_faces` guest path against WED-2024.
- **Result: it matches.** Face detected (score 0.82), cluster `9bf05c4e` at best similarity **0.58**, 11 true faces at 0.42–0.58, nearest stranger cluster at 0.31. Clean 0.31→0.42 gap ⇒ current `GUEST_MATCH_THRESHOLD=0.35` matches him with margin and excludes strangers. **Kept 0.35** (respects the ≥0.32 privacy floor). Not a threshold problem; his face IS in the index. Earlier failure = the already-fixed 3am cron index-wipe + pre-fix state.
- Tested and **DISPROVEN**: hypothesis that iPhone EXIF orientation 6 (sideways pixels) broke detection — raw sideways vs. EXIF-corrected upright both detect+match near-identically. No embed-service (`main.py`) change made. Good that it was empirically tested before shipping a fix.
- **(1) embed-status after the 3am cron: intact — 286 face_embeddings (was 262), NOT wiped.** Unlink-not-delete cron holds.
- **(2) DB-guard hardening:** wrote `supabase/migrations/0005_face_embeddings_delete_guard.sql` — `before delete` trigger on `face_embeddings` rejects any delete while the parent photo still exists unless a txn-local opt-in flag is set; guest-scoped cleanup deletes (the 2026-07-09 bug) now raise. Photo-cascade + force re-embed stay allowed (force re-embed routed through new SECURITY DEFINER `admin_clear_faces_for_photos()` RPC). Worker force-backfill updated to call the RPC with a safe fallback pre-apply. Deployed `oura-api` version `37f1a25b`; typecheck clean. Applying the DDL needs a founder Supabase Management PAT (founder opted to paste one for live apply+verify).
- **0005 APPLIED + VERIFIED LIVE** (founder-pasted Management PAT, applied via api.supabase.com — needed a browser User-Agent, the default urllib UA hits Cloudflare 1010). Verified: guest-scoped delete RAISES the guard; `admin_clear_faces_for_photos([])` returns 0 clean; photo-cascade delete allowed (rollback-tested, no data lost). embed-status still 286. PAT can be revoked.

## 2026-07-12: master Design-to-Code index + design-driven PRD (PR #30); design-index PR pileup reconciled

- **PR #30** (`claude/design-code-index-prd-5vn8xd`, docs-only): built the master
  Design-to-Code index in `docs/ARCHITECTURE.md` §6b — every `design/screens/*/screen.png`
  → its code file path(s) + wiring status (Real / static / not-built-Phase-N),
  the design-spec source files (`oura_design_specifications_final.md` = the master
  flow = leading build order), and the flow-order build sequence. Rewrote `PRD.md`
  as a design-to-code PRD (design-spec flow = source of truth for UI *and* build
  order; each screen = 1:1 wiring of its screen.png). Added `CLAUDE.md` "Session
  Budget Discipline" section. Anti-drift clause (§11) now requires updating §6b in
  the same commit as any screen move/rename/wire.
- **Design-index PR pileup reconciled (founder-approved):** three PRs were all
  about "index the design" — this session's #30 (canonical map in ARCHITECTURE
  §6b), **#29** (fixes the dead `{{DATA:SCREEN:SCREEN_###}}` tokens in
  `design/oura_final_production_index_42_screens.md` → real on-disk paths), and
  **#13** (a separate `docs/DESIGN_INDEX.md` screen⇆route + nav-gap map).
  Decision: **merge #29** (complementary — fixes a genuinely broken file #30 never
  touched), **fold #13's nav-gap findings into §6b and close #13** (its location is
  superseded by the canonical §6b), **merge #30**. All docs-only — no deploy needed
  (no CI/CD; merging to main is the whole effect).
- Confirmed the leading principle is now written down where work will read it:
  design is king, code is 1:1 with the Stitch screen.png, design-spec flow leads
  build order; if code and screenshot ever disagree, the screenshot wins.

## 2026-07-12 (cont.): create-event MOBILE bottom-sheet wired (PR #33, live)
Founder caught a real gap: `/admin/create-event` was built desktop-only and
shipped without asking where the mobile version was. He supplied the mobile
Stitch export; wired it as a responsive layout on the same route (desktop card
`hidden md:flex`; mobile bottom-sheet below `md`), shared submit via separate
`<form>`s. Founder decisions: removed the export's auto-barcode toggle (a
code+QR is always generated — no opt-out path, would've been a dead control),
and rebuilt the export's Google-CDN preview photo as a bundled CSS phone +
`qr_code_2` card (CLAUDE.md bans CDN assets). Verified: tsc exit 0, eslint
clean, prod build passes, authenticated 390px Playwright screenshot + real-DOM
`getBoundingClientRect` RTL measurement (throwaway service-role account + injected
`@supabase/ssr` cookies, since the sandbox browser can't reach Supabase auth).
Deployed oura-web version `ab53201d-3a7c-46a3-873c-cf6658498f53`. Live (auth-gated):
https://oura-web.oura-events.workers.dev/admin/create-event (view on a phone / <768px).
Lesson logged: when a design covers only one breakpoint, STOP and ask — don't ship half.

## 2026-07-13: /admin/branding wired to desktop_3 + dedicated mobile_3 (deployed)
Continuation of the branding fidelity audit (PR #34, docs-only mapping, sibling branch
`claude/branding-fidelity-audit-mei1jn`). Founder decisions: Q1 desktop_3 canonical,
Q2 build dedicated mobile_3. Wired `apps/web/app/admin/branding/page.tsx`: desktop_3
wording/section renames (`הגדרות חשבון פלטינום`, `ניהול מיתוג:`, `תצוגה מקדימה ללקוח` +
subtitle, `לוגו הסטודיו`+`שקוף מומלץ`+`העלה לוגו חדש`+WEBP, `סגנון תצוגת תמונות`,
silver→`כסף פלטינום`, `זהות המותג`, `סימן מים חכם (Watermark)`, `החלף תמונה לדוגמה:`
thumb label) for `md+`; a dedicated mobile_3 block below `md` (preview card w/ `פעיל`
badge, logo dropzone, `שם הסטודיו`→`branding.studio_name`, single brand color, full-width
save/cancel) rendered inside AdminShell per the PR #33 precedent (no forked bottom-tab
chrome). Frame/watermark/title/caption are desktop-only, faithful to the simpler mobile_3.
Text/label drift + the new mobile layout only — no invented features; hoisted the logo
file-input to top level so both dropzones share it. Verified: tsc/eslint clean, 7/7
media-ui-verify, authenticated Playwright screenshots at 1280px + 390px, real-DOM
`getBoundingClientRect` RTL correct on both breakpoints (throwaway service-role account +
injected `@supabase/ssr` cookie, per the documented sandbox method; the browser can't reach
Supabase so event data renders as "טוען…" — a data blind spot, not a layout bug). apps/api
unchanged (not redeployed). Deployed oura-web version `3a221d6a-2a38-4bf8-8053-2841eee24070`.
Live: https://oura-web.oura-events.workers.dev/admin/branding

## 2026-07-14: photo-editor 4 fixes verified + deployed (branch claude/wed-2024-face-match-t4wre2, PR #28 already merged)
- Verified all 4 founder fixes already committed in 4cdd9cd: Oura credit absent (grep+DOM), frame-off clean export, logo enlarged, design-faithful.
- Deployed oura-web version bdb7b3ad. SUMMARY updated to reflect done state.
