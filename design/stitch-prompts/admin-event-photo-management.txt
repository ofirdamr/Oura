Design a mobile screen (RTL, Hebrew) for a photographer's admin panel: the single-event photo management screen. Dark theme, coral accent (#ff8a75), Manrope headlines, Inter body, rounded-8 corners. All text is Hebrew and right-to-left.

Top header: large event title placeholder "חתונת דנה ויוסי", small grey subtitle beneath it "ניהול תמונות האירוע: העלאה, צפייה ומחיקה". To the header's start-side, two small pill buttons in a row: "מיתוג ולוגו" and "קוד QR וגישה", each with a small leading icon.

Section 1 card titled "העלאת תמונות (שלב 1)". Inside it a large dashed-border drop zone with a centered upload cloud icon and the text "גרור תמונות וסרטונים לכאן או לחץ להעלאה", and a smaller grey line under it "JPEG, PNG, MP4, MOV — ניתן גם לגרור קובץ ZIP עם כל הקבצים". Show a subtle coral progress bar state variant beneath (label "מייעל ומעלה נכסים בצורה בטוחה...").

Section 2 card titled "סנכרון תמונות ברזולוציה גבוהה (שלב 2)" with a grey helper line "העלו את הקבצים המקוריים בברזולוציה גבוהה להדפסה ומגנטים". This section has TWO tabs/modes at its top: an active tab "כל המקורים" and a second tab "רק הזמנות הדפסה (3)" with a small coral badge.

For the "כל המקורים" (bulk) mode: a single dashed drop zone that accepts many files or a ZIP at once, icon + text "גרור את כל המקורים בבת אחת (תמונות או סרטונים)", plus a compact horizontal strip of small photo THUMBNAILS below it, each thumbnail carrying a tiny status badge — an amber "שלב 1" badge for photos still awaiting their high-res original, and a green "סונכרן ✓" badge for ones already synced. Show a small counter "12 מתוך 40 סונכרנו".

Also show a highlighted callout card for the second mode: a coral-tinted card with a lightning/clock icon and title "סנכרון מהיר להזמנות חיות", helper "לקוחות שכבר הזמינו הדפסה — סנכרנו רק את המקורים שלהם כדי לספק במהירות עוד במהלך האירוע". Below it a short vertical list of 3 rows, each row = a small photo thumbnail + the guest name (e.g. "דנה כהן", "יוסי לוי", "מיכל בר") + a small pill "הזמנת הדפסה ממתינה" + a coral "סנכרן מקור" button on the row's start side. One row shows a done state: green "סונכרן ✓" instead of the button.

Section 3 titled "תמונות האירוע (40)". A responsive grid of square photo thumbnails, 3 per row on mobile. Each thumbnail shows: a small round delete (trash) icon button in one corner, a small stage badge (amber "שלב 1" or green "סונכרן ✓") in another corner, and along the bottom a small pill showing the photo's category label (e.g. "חופה", "הזוג", "משפחה", "ריקודים", "קבלת פנים", "מנה עיקרית", "אולם") with a tiny tag icon; thumbnails with no category show a faint "הוסף קטגוריה" pill instead. A small video thumbnail variant shows a play-triangle overlay in its center.

Include one category-picker bottom-sheet modal overlay variant titled "שיוך קטגוריה לתמונה". At the TOP of this sheet show the selected photo LARGE as a preview (a wide rounded image thumbnail of the photo being tagged) so it is clear which photo is being edited, with its current category name shown beneath the preview. Below the preview list the seven category chips (חופה, הזוג, משפחה, ריקודים, קבלת פנים, מנה עיקרית, אולם) with the currently-selected one highlighted in coral, plus a "הסרת קטגוריה" option and a close X.

Include one delete-confirmation modal overlay variant: a small centered dark card with a red-tinted trash icon at top, title "למחוק את התמונה?", body "הפעולה בלתי הפיכה. התמונה תוסר מהגלריה של האורחים לצמיתות.", and two buttons — a neutral "ביטול" and a red/destructive "מחק תמונה".

Overall: clean, premium, dark, generous spacing, coral primary buttons, feels like a modern photo-management dashboard on a phone.
